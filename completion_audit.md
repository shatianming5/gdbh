# 当前目标完成度审计

审计日期：2026-05-25

## 用户目标

“帮我证明强哥德巴赫猜想。”

具体成功标准：

```text
对每个偶数 N > 2，严格证明存在素数 p 和 q，使得 N = p + q。
```

## Prompt 到产物映射

| 要求 | 当前证据 | 是否满足 |
| --- | --- | --- |
| 证明覆盖所有偶数 `N > 2` | 当前没有完整证明草稿；`README.md` 只记录问题边界 | 否 |
| 证明存在两个素数表示 | `verify_goldbach.py` 可在有限上界内寻找见证 | 仅有限满足 |
| 避免把有限计算当成证明 | `README.md`、`references.md`、脚本输出都明确说明有限验证不是证明 | 是 |
| 能审查后续证明尝试 | `proof_audit.md` 提供量词、引理、区间、假设、交集论证检查项 | 是 |
| 提供可复现实验起点 | `verify_goldbach.py` 和 `test_verify_goldbach.py` 已存在 | 是 |
| 建立形式化拼接框架 | `Gdbh/Goldbach.lean` 定义有限证书、解析桥接接口和条件拼接定理 | 是，无本地 axiom |
| 让有限证书被 Lean 内核复核 | `verify_goldbach.py --export-lean` 生成 `Gdbh/Certificate100.lean`；chunked 生成 `Gdbh/Certificate50000.lean`，`lake build` 关键目标类型检查通过 | 是，当前最强有限证书覆盖到 50000 |
| 让有限证书可复现 | `verify_goldbach.py --export-manifest` 生成 `certificate_manifest.json`、`cert10000_manifest.json`、`cert20000_manifest.json` 和 `cert50000_manifest.json`，测试确认 manifest、证书和生成器输出一致 | 是 |
| 记录解析交接证书字段 | `analytic_handoff_certificate.py` 校验完全显式 HL weight-sum、canonical weight-sum、quarter canonical weight-sum、quarter explicit-contamination canonical weight-sum、quarter lower-bound explicit-contamination canonical weight-sum、positive-linear raw canonical weight-sum、positive-linear raw explicit-contamination canonical weight-sum、positive-linear canonical major/minor、positive-linear explicit-contamination canonical major/minor、quarter major/minor decomposition、quarter linear-error decomposition、DFT-level L2 minor-arc、uniform-minor DFT、uniform-minor-square DFT、direct positive-linear uniform-minor-square DFT explicit-contamination 和 fixed-square-error direct positive-linear uniform-minor-square DFT explicit-contamination 十六类证书模板，模板均报告 `valid: true`、`estimate_complete: false`、`complete: false`；旧模板可用 `--check-formalized-lean` 核对 Lean 已证明的 quarter-base singular-series 下界和 canonical sqrt-log weight-sum 上界局部义务；新的 quarter canonical 模板把 `goldbachSingularSeriesFromQuarter`、系数 `1/4` 和 `one_fourth_le_goldbachSingularSeriesFromQuarter` 固定在 Lean 对象内，只留下该固定主项下的 HL 归一化误差解析义务和最终 `canonicalContaminationThresholdBound` 覆盖义务；quarter explicit-contamination canonical 模板保留该固定主项下的 HL 归一化误差解析义务，并额外要求显式 `contamination_dominated`，用 `threshold` 和 `contaminationThreshold` 合成最终阈值；quarter lower-bound explicit-contamination 模板只要求一边 `rawRelativeLowerBound` 与显式 `contaminationDominated`，不要求 `rawNormalizedErrorBound` 的上侧误差；positive-linear raw canonical 模板把证书面压到 `coefficient*n <= RawVonMangoldtGoldbachSum n` 和最终 `canonicalContaminationThresholdBound`；positive-linear raw explicit-contamination canonical 模板把证书面压到 `coefficient*n <= RawVonMangoldtGoldbachSum n` 与显式 `contamination_dominated`，用 `rawThreshold` 和 `contaminationThreshold` 合成最终阈值，避免覆盖 noncomputable `canonicalLinearContaminationThreshold`；positive-linear canonical major/minor 模板把证书面压到 `mainTerm - majorArcError <= Raw + minorArcError` 与 `coefficient*n + minorArcError <= mainTerm - majorArcError` 两条逐点不等式，并由 Lean 消去 minor error 与接入 canonical 污染预算；positive-linear explicit-contamination canonical major/minor 模板额外要求显式 `contamination_dominated`，从而用数值 `contaminationThreshold` 合成最终阈值，避免覆盖 noncomputable `canonicalLinearContaminationThreshold`；decomposition 模板把 R2/R3 拆成 raw decomposition、major arc 近似、minor arc 贡献界和 total error 支配四个 Lean 义务，并把最终交接细化为四个数值解析阈值自动覆盖加 `canonicalContaminationThresholdBound` 污染阈值覆盖；linear-error 模板进一步允许 total error 以 `epsilon*n` 给出，并由 Lean 检查 `epsilon <= relativeError/4`；DFT/L2 模板把当前最贴近圆法的 major DFT-square 模型、minor DFT 点态界、minor L2 平方和界和 total linear error 义务做成可导出 Lean wrapper 的证书入口；uniform-minor DFT 模板把 minor 义务降成统一 DFT 上界、频率数量上界和一个标量平方误差不等式；uniform-minor-square DFT 模板用平凡频率数上界和平方型 uniform-bound 桥把 minor 义务进一步压成统一 DFT 上界与 `M(n)^2 <= minorArcError(n)`，不再要求单独证明非负性；direct positive-linear DFT explicit-contamination 模板进一步要求 `majorArcLinearLowerBound` 和显式 `contamination_dominated`，用三个数值阈值直接合成最终阈值，避免覆盖 noncomputable `canonicalLinearContaminationThreshold`，且当 `contaminationThreshold >= 2` 时可从 `contamination_sqrt_log_model_bound` 通过已形式化的 `sqrt(n)*log(n)^3` 比较定理自动导出显式污染支配；fixed-square-error direct DFT 模板把 minor 预算固定为 `M(n)^2`，删除独立 `minorArcError` 字段和 `minor_arc_dft_bound_sq_error_bound` 义务；canonical 污染支配阈值现在暴露为 `canonicalHLContaminationThreshold` 和 `canonicalLinearContaminationThreshold` 并接入 split-threshold direct raw weight-sum 桥；`--check-lean` 现在会同时类型检查解析义务和生成的 handoff wrapper，从而检查最终阈值证明项；模板还记录 `finiteCertificateTheorem`，以便完整证书导出无参数 `StrongGoldbach` theorem | 是，模板不是证明 |
| 支持分块有限证书 | `Gdbh/FiniteIntervals.lean` 定义 `GoldbachBetween A B` 和区间证书检查器；`verify_goldbach.py --interval-start` 与 `--chunk-size` 可导出区间 Lean 证书；当前 chunked 证书证明 `Gdbh.goldbachBetween2To50000` 和 `Gdbh.goldbachUpTo50000_of_chunkedCertificate2To50000`；从 `--interval-start 2` 开始的自包含输出会导出 `explicitLowerBound100_from_*_and_explicit_lower_bound`，可由 `GoldbachUpTo B`、`T ≤ B` 和 `ExplicitGoldbachLowerBound T` 直接得到字面目标 `ExplicitGoldbachLowerBound 100`；自包含输出还生成最终 `StrongGoldbach` 条件桥，覆盖显式下界、圆法、major/minor arc、计数正性、带权下界、带权 major/minor arc、污染项扣除下界、污染项扣除 major/minor arc、实数污染项扣除下界、实数污染项扣除 major/minor arc、von Mangoldt 下界、von Mangoldt major/minor arc、von Mangoldt 素数幂污染项下界、von Mangoldt 素数幂污染项 major/minor arc、左右拆分素数幂污染项下界、左右拆分素数幂污染项 major/minor arc、逐点左右拆分素数幂污染项下界、逐点左右拆分素数幂污染项 major/minor arc、统一上界左右拆分素数幂污染项下界、统一上界左右拆分素数幂污染项 major/minor arc、计数归约左右拆分素数幂污染项下界、计数归约左右拆分素数幂污染项 major/minor arc、平凡计数上界左右拆分素数幂污染项下界、平凡计数上界左右拆分素数幂污染项 major/minor arc、因子上界左右拆分素数幂污染项下界和因子上界左右拆分素数幂污染项 major/minor arc 接口；内置 `Certificate100` 保持低层，配套字面目标桥在 `Gdbh/AnalyticBridge.lean` 中给出 | 是 |
| 精确表达证明缺口 | `Gdbh/Goldbach.lean` 证明了当 `2 ≤ B` 时，`StrongGoldbach ↔ GoldbachUpTo B ∧ GoldbachAbove B`；`Gdbh/Certificate100.lean` 进一步证明 `StrongGoldbach ↔ GoldbachAbove 100` | 是 |
| 细化无限区间目标 | `Gdbh/Goldbach.lean` 定义 `GoldbachCount` 并证明 `GoldbachCount n > 0 ↔ GoldbachRepresentation n`；`Gdbh/Certificate100.lean` 证明 `StrongGoldbach ↔ GoldbachCountPositiveAbove 100` | 是 |
| 建立解析证明接口 | `Gdbh/AnalyticBridge.lean` 定义 `ExplicitGoldbachLowerBound 100` 并证明它可推出且等价于当前剩余目标 | 是 |
| 启动奇异级数下界形式化 | `Gdbh/SingularSeries.lean` 证明有限 odd-prime 局部修正因子乘积 `goldbachSingularSeriesLocalMultiplier n` 不小于 `1`，证明非负 base 项乘以该局部乘子不会低于 base，并给出当前证书可直接引用的 `goldbachSingularSeriesFromQuarter` 与 `one_fourth_le_goldbachSingularSeriesFromQuarter`；raw 归一化误差义务仍必须匹配同一个 `singularSeries` 归一化 | 当前证书义务已形式化 |
| 建立条件证明接口 | `Gdbh/Conditional.lean` 定义条件证明程序，形式化说明若 GRH/Hardy-Littlewood 型假设给出显式下界，则可推出强哥德巴赫 | 是，条件接口不是无条件证明 |
| 建立圆法充分条件接口 | `Gdbh/CircleMethod.lean` 定义主项、误差项、计数下界和误差支配条件，并证明阈值不超过 100 时可推出强哥德巴赫 | 是，尚未给出具体解析估计 |
| 细化圆法 major/minor arc 接口 | `Gdbh/MajorMinorArcs.lean` 定义 `MajorMinorArcEstimate`，并证明它可转成 `CircleMethodLowerBound` 以及阈值不超过 100 时可推出强哥德巴赫 | 是，尚未给出具体 major/minor arc 估计 |
| 建立带权表示数桥接接口 | `Gdbh/WeightedGoldbach.lean` 定义素数支撑权重和 `WeightedGoldbachLowerBound`，证明带权和为正可推出普通 Goldbach 表示 | 是，尚未给出具体带权解析下界 |
| 连接带权接口和当前计数目标 | `Gdbh/WeightedGoldbach.lean` 定义 `primeIndicatorWeight`，并证明其带权和等于 `GoldbachCount`，从而带权正性目标等价于 `ExplicitGoldbachLowerBound B` | 是 |
| 建立带权 major/minor arc 接口 | `Gdbh/WeightedMajorMinorArcs.lean` 定义 `WeightedMajorMinorArcEstimate`，并证明它可转成 `WeightedGoldbachLowerBound` 以及阈值不超过 100 时可推出强哥德巴赫 | 是，尚未给出具体带权 major/minor arc 估计 |
| 建立污染项扣除接口 | `Gdbh/ContaminatedWeightedGoldbach.lean` 定义原始带权卷积、素数对部分、非素数对污染项、污染项扣除下界和污染项扣除 major/minor arc 估计；Lean 证明总带权和严格大于非素数对贡献时可推出普通 Goldbach 表示 | 是，尚未给出具体 von Mangoldt/素数幂型解析估计 |
| 建立实数污染项扣除接口 | `Gdbh/RealContaminatedWeightedGoldbach.lean` 定义 `Nat → ℝ` 非负权重的原始卷积、素数对部分、非素数对污染项、实数下界和实数 major/minor arc 估计；Lean 证明实数总带权和严格大于非素数对贡献时可推出普通 Goldbach 表示 | 是，尚未给出具体实数解析估计 |
| 建立 von Mangoldt 专用接口 | `Gdbh/VonMangoldtGoldbach.lean` 使用 mathlib `ArithmeticFunction.vonMangoldt` 定义 `vonMangoldtWeight`，证明 `vonMangoldtWeight_nonneg`，定义 `VonMangoldtGoldbachLowerBound` 与 `VonMangoldtMajorMinorArcEstimate`，证明非素数对污染项等于素数幂污染项，并证明素数幂污染项可由左右两侧污染项控制，还证明左右污染项可由逐点上界和有限和上界控制，证明统一逐项上界乘以污染集合个数上界足以控制污染和，证明左右污染集合个数都不超过 `NonPrimePrimePowerCount n`，证明 `NonPrimePrimePowerCount n <= n + 1`，证明更强的 `NonPrimePrimePowerCount n <= (sqrt n + 1)*(log₂ n + 1)`，证明两个 `Λ` 因子各自有上界时其乘积由上界平方控制，用 mathlib 证明 `Λ(m) <= log n`，固定左右污染预算，新增保留显式 `countBound` 的 log 污染预算，并新增直接 raw Λ 卷积下界接口；quarter decomposition 路线现在还支持把 `majorArcError + minorArcError <= epsilon*n` 线性 total-error 输出自动转成主项相对误差义务；Lean 证明这些对象可推出普通 Goldbach 计数正性和最终强猜想 | 是，尚未给出 raw Λ 卷积下界 |
| 建立有限 Fourier raw decomposition | `Gdbh/DiscreteCircleMethod.lean` 证明 `ZMod N` 上的卷积 DFT 恒等式、inverse DFT 频率和公式、任意 major/minor 频率分块恒等式，并专门化为 `RawVonMangoldtGoldbachSum n` 的 real-valued major/minor decomposition；新的 `VonMangoldtFourierQuarterLinearErrorCanonicalWeightSumEstimate` 自动把 `raw_decomposition` 关闭，只留下 major arc 近似、minor arc 贡献界、线性总误差界和污染阈值覆盖；`VonMangoldtFourierComplexQuarterLinearErrorCanonicalWeightSumEstimate` 进一步允许 major/minor 估计以复数范数形式给出，由 Lean 转成实部估计；Fourier 项已命名成显式 DFT 平方项 `1/(n+1) * e(kn/(n+1)) * S(k)^2`，并证明 major 复贡献可由逐频率 DFT 平方项近似模型项、模型和接近 `goldbachSingularSeriesFromQuarter(n) * n` 以及误差求和推出 major arc 近似界；minor 复贡献范数可由这些项在 minor 频率集合上的逐项上界求和控制；也可直接由 `‖S(k)‖` 型统一上界推出 `‖Minor‖ <= M(n)^2`，再与 `M(n)^2 <= minorArcError(n)` 拼成 minor 复贡献界；还可用 L2 型平方和桥，把 `(1/(n+1))*sum_minor ‖S(k)‖^2` 或频率依赖上界 `(1/(n+1))*sum_minor B(k)^2` 接成 minor 复贡献界；`VonMangoldtDftModelL2MinorQuarterLinearErrorCanonicalWeightSumEstimate` 进一步把 major 模型项、minor L2 平方和与 total linear error 字段打包成可直接转入复数 Fourier handoff 的 DFT 级入口；`VonMangoldtDftModelUniformMinorQuarterLinearErrorCanonicalWeightSumEstimate` 把 minor L2 字段降为统一 DFT 上界、minor 频率数上界和一个标量平方误差不等式；`VonMangoldtDftModelUniformMinorSqQuarterLinearErrorCanonicalWeightSumEstimate` 使用平凡频率数上界，把 minor 字段进一步压成 `M(n)^2 <= minorArcError(n)`；`VonMangoldtDftUniformMinorSqPositiveLinearCanonicalLowerBound` 进一步提供正线性 DFT 捷径，从 `Major >= coefficient*n + minorError` 和同一套统一 minor DFT 界直接得到 raw Λ 正线性下界；`VonMangoldtDftUniformMinorSqPositiveLinearExplicitContaminationCanonicalLowerBound` 则用显式 `contaminationThreshold` 和 `contaminationDominated` 替代 noncomputable canonical 污染阈值覆盖；`VonMangoldtDftUniformMinorSqFixedErrorPositiveLinearExplicitContaminationCanonicalLowerBound` 把 minor error 固定为 `M(n)^2`，让 Lean 以 `rfl` 关闭平方误差义务 | 是，尚未给出 major/minor 估计 |
| 泛化有限证书和解析阈值拼接 | `Gdbh/GeneralHandoff.lean` 证明 `GoldbachUpTo B` 可与任意阈值 `T ≤ B` 的解析下界组合推出强哥德巴赫；新增 `explicit_lower_bound100_of_finite_and_explicit_lower_bound_le`，同样的数据可直接推出字面剩余目标 `ExplicitGoldbachLowerBound 100`，生成的完整解析 wrapper 会额外导出 `explicitLowerBound100_from_*` | 是 |
| 将圆法接口接入证书生成器 | `verify_goldbach.py --export-lean` 生成证书时自动导出“圆法阈值不超过证书上界即可推出强哥德巴赫”的定理 | 是 |
| 审计 Lean 源码假设/占位符 | `audit_lean_source.py` 扫描 `Gdbh.lean` 和 `Gdbh/**/*.lean`，忽略注释/字符串后禁止 `axiom`、`constant`、`opaque`、`unsafe`、`sorry`、`admit` | 是 |
| 审计关键 Lean 定理依赖 | `audit_lean_axioms.py` 使用 `#print axioms` 检查关键定理只依赖允许的 Lean/mathlib 基础公理；这不是文本匹配，而是 Lean 环境级依赖检查 | 是 |
| 机器可读地记录完成状态 | `proof_status.json` 明确记录 `complete: false`、剩余目标和可执行核验命令 | 是 |
| 机器可读地记录阻塞义务 | `proof_status.json` 顶层 `blocking_obligations` 明确列出当前剩余 Lean 目标、canonical HL 路线缺少的 `raw_normalized_error_bound`、`derived_threshold_bound` 两个证明项，记录 quarter-base `singular_series_lower_bound` 已由 Lean 检查，保留备用显式 weight-sum 路线和最终 `--require-complete --check-lean` 门禁 | 是 |
| 逐阶段攻坚路线 | `proof_status.json` 顶层 `attack_plan` 和 `analytic_attack_plan.md` 按优先级记录 singular series、显式 canonical 阈值、raw estimate ledger、major arc、minor arc、有限证书扩展和最终 handoff 的交付物与验收门禁 | 是，路线图不是证明 |
| 形式化有限商/模覆盖障碍 | `Gdbh/FiniteQuotientObstruction.lean` 证明固定有限界内的模 pair-sum 覆盖可以同时成立，而自然数 pair-sum 覆盖仍在所有足够大目标上失败；`proof_status.json` 记录该 Lean 障碍说明 | 是，障碍说明不是证明 |
| 形式化有限例外集障碍 | `Gdbh/ExceptionalSetObstruction.lean` 证明 binary-Goldbach-shaped 全称覆盖等价于阈值内有限检查加阈值外覆盖；也证明 one-hole 谓词可在阈值外全成立但仍不全称成立 | 是，障碍说明不是证明 |
| 形式化有限前缀障碍 | `Gdbh/FinitePrefixObstruction.lean` 证明任意长的有限前缀完全检查，包括当前 50000 阈值，本身仍不能推出 binary-Goldbach-shaped 全称命题；还需要无限尾部定理 | 是，障碍说明不是证明 |
| 形式化 K-Goldbach 形状障碍 | `Gdbh/KGoldbachShapeObstruction.lean` 证明 bounded-list coverage、精确 two-pair-sumset coverage `(A+A)+(A+A)` 和允许 `0`/`1` padding 的 pair coverage 都不能逻辑推出 unpadded binary pair coverage；现在还显式证明任意 `K ≥ 1`、特别是 Ramaré 型 `K = 6` 覆盖形状本身不能推出二元 pair coverage，并强化为：即使 `(A+A)+(A+A)` 覆盖所有正整数且 `0,1 ∈ A`，仍不能推出 binary pair coverage；`proof_status.json` 记录该 Path C 终局差距 | 是，障碍说明不是证明 |
| 形式化 Path C 半密度形状交付 | `Gdbh/PathC_HalfDensityShape.lean` 证明如果 `primesSumset` 的 Schnirelmann density 至少为 `1/2`，则每个正整数都有至多四个 `primesAndOne` 加数表示；这精确记录已闭合半密度步骤的输出形状，但仍允许四项和 `0`/`1` padding，不是 binary Goldbach | 是，形状交付不是二元证明 |
| 形式化 ternary/weak Goldbach 形状障碍 | `Gdbh/TernaryGoldbachObstruction.lean` 给出模 8 反例：`{1,3}` 三项覆盖所有奇 residue，但二项不能覆盖偶 residue `0`；`proof_status.json` 记录该 ternary-to-binary 终局差距 | 是，障碍说明不是证明 |
| 形式化 Chen 型形状障碍 | `Gdbh/ChenShapeObstruction.lean` 证明即使 `Broad` 严格扩展 `Core`，全域 `Core + Broad` 表示也不能逻辑推出全域 `Core + Core` 表示；`proof_status.json` 记录 prime-plus-almost-prime 到 binary-prime-pair 的终局差距 | 是，障碍说明不是证明 |

## 已执行核验

```bash
python3 -m unittest -v test_analytic_handoff_certificate.py
python3 generate_proof_status.py
python3 -m unittest -v test_proof_status.py
python3 analytic_handoff_certificate.py analytic_handoff_certificate.example.json
python3 analytic_handoff_certificate.py analytic_handoff_certificate.example.json --check-formalized-lean
python3 analytic_handoff_certificate.py analytic_canonical_handoff_certificate.example.json
python3 analytic_handoff_certificate.py analytic_quarter_canonical_handoff_certificate.example.json
python3 analytic_handoff_certificate.py analytic_quarter_explicit_contamination_canonical_handoff_certificate.example.json
python3 analytic_handoff_certificate.py analytic_quarter_lower_bound_explicit_contamination_canonical_handoff_certificate.example.json
python3 analytic_handoff_certificate.py analytic_positive_linear_raw_canonical_handoff_certificate.example.json
python3 analytic_handoff_certificate.py analytic_positive_linear_raw_explicit_contamination_canonical_handoff_certificate.example.json
python3 analytic_handoff_certificate.py analytic_positive_linear_canonical_major_minor_handoff_certificate.example.json
python3 analytic_handoff_certificate.py analytic_positive_linear_explicit_contamination_canonical_major_minor_handoff_certificate.example.json
python3 analytic_handoff_certificate.py analytic_decomposition_handoff_certificate.example.json
python3 analytic_handoff_certificate.py analytic_linear_decomposition_handoff_certificate.example.json
python3 analytic_handoff_certificate.py analytic_dft_model_l2_handoff_certificate.example.json
python3 analytic_handoff_certificate.py analytic_dft_model_uniform_minor_handoff_certificate.example.json
python3 analytic_handoff_certificate.py analytic_dft_model_uniform_minor_sq_handoff_certificate.example.json
python3 analytic_handoff_certificate.py analytic_dft_uniform_minor_sq_positive_linear_explicit_contamination_handoff_certificate.example.json
python3 analytic_handoff_certificate.py analytic_dft_uniform_minor_sq_fixed_error_positive_linear_explicit_contamination_handoff_certificate.example.json
python3 verify_goldbach.py 100000
python3 analyze_von_mangoldt.py 1000 --coefficient 1.0 --relative-error 0.5
python3 -m py_compile analyze_von_mangoldt.py verify_goldbach.py analytic_handoff_certificate.py test_verify_goldbach.py test_no_project_axioms.py test_certificate_manifest.py test_lean_axiom_audit.py test_generated_lean_certificates.py test_proof_status.py test_analytic_handoff_certificate.py audit_lean_source.py audit_lean_axioms.py generate_proof_status.py
python3 verify_goldbach.py 100 --export-lean Gdbh/Certificate100.lean --export-manifest certificate_manifest.json
python3 audit_lean_source.py
python3 audit_lean_axioms.py
source "$HOME/.elan/env" && lake build
lake build Gdbh.FiniteQuotientObstruction
lake build Gdbh.ExceptionalSetObstruction
lake build Gdbh.FinitePrefixObstruction
lake build Gdbh.CounterexampleReduction
lake build Gdbh.KGoldbachShapeObstruction
```

结果：

- 解析交接证书测试：新增 quarter explicit-contamination canonical 路线的模板校验、完整性校验、Lean 渲染、义务 probe 和 mock Lean wrapper 检查通过；新增 uniform-minor-square DFT 证书的模板校验、完整性校验、Lean 渲染、义务 probe 和 mock Lean wrapper 检查通过，把最粗 minor arc 交接精确压成统一 DFT 上界和 `M(n)^2 <= minorArcError(n)`，并已消去单独的非负性字段；fixed-square-error direct DFT positive-linear explicit-contamination 路线也已加入证书模板、Lean 渲染和 mock wrapper 检查，它把 minor 预算固定成 `M(n)^2`，删除独立 `minorArcError` 字段和 `minor_arc_dft_bound_sq_error_bound` 义务。污染项侧新增 `one_le_real_log_nat_of_three_le` 和 `quarter_canonicalNonPrimePrimePowerVonMangoldtWeightSumBound_contamination_dominated_of_sqrt_log_model_ge_two_threshold`，可把 `contamination_dominated` 进一步规约为 `contaminationThreshold >= 2` 加显式 `K * sqrt(n) * log(n)^3` 线性支配模型界。
- 一边 Hardy-Littlewood 下界入口：Lean 新增 `VonMangoldtQuarterHardyLittlewoodLowerBoundExplicitContaminationCanonicalWeightSumEstimate`，把 quarter-base 主项固定为 `goldbachSingularSeriesFromQuarter(n) * n`，但只要求解析侧证明一边下界 `(1 - relativeError) * main <= RawVonMangoldtGoldbachSum n` 和显式 canonical 污染预算支配；Lean 自动用 `one_fourth_le_goldbachSingularSeriesFromQuarter` 转成正线性 raw Λ 下界，并由 `threshold` 与 `contaminationThreshold` 合成最终阈值。该入口已进入 `proof_status.json`、公理审计和 JSON 证书生成器。
- 一边 major/minor 下界入口：Lean 新增 `raw_relative_lower_bound_of_major_minor_lower_bound` 和 `VonMangoldtQuarterMajorMinorLowerBoundExplicitContaminationCanonicalWeightSumEstimate`，把 `raw_relative_lower_bound` 再拆成 `Raw = Major + Minor`、`Major >= (1-relativeError)*main + minorArcError`、`Minor >= -minorArcError` 与显式 canonical 污染预算支配；这去掉了 full absolute-error route 中不必要的 major 上误差和 minor 双边绝对值要求。该入口已进入 `proof_status.json` 与公理审计，但还不是 JSON 证书模板。
- Fourier/DFT 一边 major/minor 下界入口：Lean 新增 `DiscreteCircleMethod.VonMangoldtFourierQuarterMajorMinorLowerBoundExplicitContaminationCanonicalWeightSumEstimate`、`VonMangoldtDftUniformMinorSqQuarterMajorMinorLowerBoundExplicitContaminationCanonicalWeightSumEstimate` 和 fixed-error 版本。Lean 现在自动给出 Fourier `Raw = Major + Minor`，把 `‖MinorComplex‖ <= minorArcError` 转成 `Minor >= -minorArcError`，并能把 minor arc 义务压成统一 DFT 上界 `‖S(k)‖ <= M(n)` 加 `M(n)^2` 误差预算；fixed-error 版本连独立 `minorArcError` 字段也去掉，并已加入 JSON 证书模板与 validator。新的 `VonMangoldtDftUniformMinorSqFixedErrorQuarterMajorMinorLowerBoundSqrtLogContaminationCanonicalWeightSumEstimate` 进一步把污染项字段从直接 `contaminationDominated` 降成 `contaminationThreshold >= 2` 和显式 `sqrt(n)*log(n)^3` 模型界。
- 证明状态测试：3 个测试通过，确认 `proof_status.json` 仍记录 `complete: false`，并追踪新增 raw explicit-contamination canonical shortcut。
- 证明状态：`proof_status.json` 已生成，记录 `complete: false`，当前最强有限归约后的剩余目标是 `Gdbh.ExplicitGoldbachLowerBound 50000`；旧的 `Gdbh.ExplicitGoldbachLowerBound 100` 目标仍由该更强目标推出。
- 有限商障碍：`Gdbh/FiniteQuotientObstruction.lean` 已通过 Lean 构建；目标定理
  `Gdbh.FiniteQuotientObstruction.finiteQuotientDataUpTo_not_global`
  的 targeted `#print axioms` 只返回
  `[propext, Classical.choice, Quot.sound]`。这说明有限商/模覆盖路线需要额外的无限解析或算术输入，不是 Goldbach 证明。
- 有限例外集障碍：`Gdbh/ExceptionalSetObstruction.lean` 已通过 Lean 构建；目标定理
  `Gdbh.ExceptionalSetObstruction.evenDomainEventuallyAbove_not_global`
  的 targeted `#print axioms` 只返回
  `[propext, Classical.choice, Quot.sound]`。这说明 density-one/cofinite/eventual 路线必须同时关闭有限例外范围，不能单独当成完整强哥德巴赫证明。
- 有限前缀障碍：`Gdbh/FinitePrefixObstruction.lean` 已通过 Lean 构建；目标定理
  `Gdbh.FinitePrefixObstruction.finitePrefixCheck50000_not_global`
  的 targeted `#print axioms` 只返回
  `[propext, Quot.sound]`。这说明把有限证书继续扩大，包括当前 50000 证书，本身仍不能代替无限尾部证明。
- Counterexample point-bound catch：`Gdbh/CounterexampleReduction.lean` 已通过 Lean 构建；新增定理
  `Gdbh.CounterexampleReduction.not_lowerHalfPrimeResidueSmallPointBoundCofactorPrimeCountingFullSplitSurplus_of_eight_le`
  、
  `Gdbh.CounterexampleReduction.no_minimalBinaryCounterexample_of_minimalCounterexampleFiniteResidueSmallPointBoundCofactorPrimeCountingFullSplitSurplus`
  和
  `Gdbh.CounterexampleReduction.minimalCounterexampleFiniteResidueSmallPointBoundCofactorPrimeCountingFullSplitSurplus_iff_no_minimalBinaryCounterexample`
  ，以及直接等价到当前尾目标的
  `Gdbh.CounterexampleReduction.minimalCounterexampleFiniteResidueSmallPointBoundCofactorPrimeCountingFullSplitSurplus_iff_explicitLowerBound50000`
  的 targeted `#print axioms` 只返回
  `[propext, Classical.choice, Quot.sound]`。这说明 point-bound two-range sufficient condition 是一个过强死端：它不能在任何 `m >= 8` 的真实候选最小反例上成立。
- Counterexample parity-aware point-bound repair：同一文件现在新增
  `Gdbh.CounterexampleReduction.BadLowerHalfPrimeSmallResidueClassParityPointBoundSum`
  与
  `Gdbh.CounterexampleReduction.MinimalCounterexampleFiniteResidueSmallParityPointBoundCofactorPrimeCountingFullSplitSurplus`，
  在偶数 `m` 下把模数 `2` 的坏同余类用至多一个点单独处理，只对其余奇素数模数使用 `m / 2 / r + 1` 点界；新增桥
  `Gdbh.CounterexampleReduction.explicitLowerBound50000_of_minimalCounterexampleFiniteResidueSmallParityPointBoundCofactorPrimeCountingFullSplitSurplus`
  已通过局部 Lean 构建。随后新增 catch
  `Gdbh.CounterexampleReduction.not_lowerHalfPrimeResidueSmallParityPointBoundCofactorPrimeCountingFullSplitSurplus_of_eight_le`
  证明这条 parity-aware 修复仍是死端：`R < 5` 时大模数 cofactor 窗口过大，`R >= 5` 时小模数 `2,3,5` 的点界贡献已经过大；对应最小反例义务也已由
  `Gdbh.CounterexampleReduction.minimalCounterexampleFiniteResidueSmallParityPointBoundCofactorPrimeCountingFullSplitSurplus_iff_no_minimalBinaryCounterexample`
  归约为“不存在最小二元反例”本身，并由
  `Gdbh.CounterexampleReduction.minimalCounterexampleFiniteResidueSmallParityPointBoundCofactorPrimeCountingFullSplitSurplus_iff_explicitLowerBound50000`
  直接等价到当前 `ExplicitGoldbachLowerBound 50000` 尾目标。
- Counterexample descent 精确死端：同一文件进一步证明
  `Gdbh.CounterexampleReduction.minimalBinaryCounterexampleDescent_iff_no_minimalBinaryCounterexample`
  、
  `Gdbh.CounterexampleReduction.minimalBinaryCounterexampleDescent_iff_currentFormalTarget50000`
  和
  `Gdbh.CounterexampleReduction.minimalBinaryCounterexampleDescent_iff_no_halfWitnessCounterexampleAbove50000`。
  这明确了 descent interface 本身也不是更弱的独立子目标：若存在 induction-ready 最小反例，任何下降都会与其最小性矛盾；反过来，不存在最小反例时该义务只是空真。
- Counterexample universal handoff 精确强度：同一文件现在新增通用定理
  `Gdbh.CounterexampleReduction.minimalCounterexampleObligation_iff_no_minimalBinaryCounterexample`
  和
  `Gdbh.CounterexampleReduction.minimalCounterexampleObligation_iff_explicitLowerBound50000`。
  它们说明任意形如“每个 induction-ready 最小反例满足 `P`”的义务，只要已有桥能从该义务关闭字面二元目标，就自动等价于“不存在 induction-ready 最小反例”和当前 `ExplicitGoldbachLowerBound 50000` 尾目标。命名实例已覆盖 finite-residue escape、finite-residue surplus、union-bound surplus、cofactor-box-product-with-`R < m` 以及 full prime-counting handoff。
- K-Goldbach 形状障碍：`Gdbh/KGoldbachShapeObstruction.lean` 已通过 Lean 构建；目标定理
  `Gdbh.KGoldbachShapeObstruction.kSummandCoverage_not_force_binaryPairCoverage`
  、`Gdbh.KGoldbachShapeObstruction.kSummandCoverage_not_force_binaryPairCoverage_of_one_le`
  、`Gdbh.KGoldbachShapeObstruction.sixSummandCoverage_not_force_binaryPairCoverage`
  、`Gdbh.KGoldbachShapeObstruction.twoPairSumsetCoverage_not_force_binaryPairCoverage`
  、`Gdbh.KGoldbachShapeObstruction.positiveTwoPairSumsetCoverage_with_zero_one_not_force_binaryPairCoverage`
  与
  `Gdbh.KGoldbachShapeObstruction.paddedPairCoverage_not_force_coreBinaryPairCoverage`
  的 targeted `#print axioms` 只返回
  `[propext, Classical.choice, Quot.sound]`。这说明 Path C 的 bounded K-summand 或 `0`/`1` padding 形态不能直接报告为 binary two-prime 终局。
  新增的 two-pair-sumset theorem 还说明，把半密度步骤应用到 pair-sumset 后得到的 `(A+A)+(A+A)` 形状本身仍不是 binary pair coverage；强化版本还要求该覆盖对所有正整数成立且允许谓词包含 `0` 和 `1`，更贴近 `primesSumset` 半密度 handoff 的输出域。
- Path C 半密度形状交付：`Gdbh/PathC_HalfDensityShape.lean` 已通过 Lean 构建；目标定理
  `Gdbh.PathCHalfDensityShape.primesAndOne_fourSummandCoverage_of_primesSumset_halfDensity`
  的 targeted `#print axioms` 只返回
  `[propext, Classical.choice, Quot.sound]`。这说明半密度步骤应用到 `primesSumset` 后的直接输出是至多四个 `primesAndOne` 加数，而不是二元素数对。
- Ternary/weak Goldbach 形状障碍：`Gdbh/TernaryGoldbachObstruction.lean` 已通过 Lean 构建；目标定理
  `Gdbh.TernaryGoldbachObstruction.ternaryOddResidueCoverage_not_force_binaryEvenResidueCoverage`
  的 targeted `#print axioms` 只返回
  `[propext, Quot.sound]`。这说明三项覆盖所有奇 residue 的信息本身不能直接报告为 binary even two-summand 终局。
- Chen 型形状障碍：`Gdbh/ChenShapeObstruction.lean` 已通过 Lean 构建；目标定理
  `Gdbh.ChenShapeObstruction.corePlusBroadCoverage_not_force_corePairCoverage`
  的 targeted `#print axioms` 只返回
  `[propext, Classical.choice, Quot.sound]`。这说明 prime-plus-almost-prime 或任何 core-plus-broad 形态本身不能直接报告为两侧都满足同一 core predicate 的 binary 终局。
- 阻塞义务清单：`proof_status.json` 顶层 `blocking_obligations` 已生成，推荐 quarter canonical HL 路线还缺 `raw_normalized_error_bound` 和 `canonical_contamination_threshold_bound`；quarter explicit-contamination canonical shortcut 还缺 `raw_normalized_error_bound` 和 `contamination_dominated`；quarter lower-bound explicit-contamination shortcut 还缺 `raw_relative_lower_bound` 和 `contamination_dominated`，它去掉了归一化误差上侧；quarter major/minor lower-bound explicit-contamination shortcut 还缺 `raw_decomposition`、`major_arc_lower_bound`、`minor_arc_contribution_lower_bound` 和 `contamination_dominated`；Fourier quarter major/minor shortcut 进一步消去 `raw_decomposition`，只剩 `major_arc_lower_bound`、`minor_arc_complex_contribution_bound` 和 `contamination_dominated`；DFT uniform-minor-square quarter major/minor shortcut 还缺 `major_arc_lower_bound`、`minor_arc_uniform_dft_bound_valid`、`minor_arc_dft_bound_sq_error_bound` 和 `contamination_dominated`；fixed-error 版本只剩 `major_arc_lower_bound`、`minor_arc_uniform_dft_bound_valid` 和 `contamination_dominated`，其中污染项可改走 Lean 原生 sqrt-log contamination handoff，替换成 `contaminationThreshold >= 2` 与 `contamination_sqrt_log_model_bound`，minor DFT 界可从标准 off-major-arcs 形式 `minor_arc_uniform_dft_bound_off_major_arcs` 派生，也可从全局非零频率界 `minor_arc_uniform_dft_bound_nonzero` 加 `zero_frequency_mem_major_arcs` 派生；positive-linear raw canonical shortcut 还缺 `raw_linear_lower_bound` 和 `canonical_contamination_threshold_bound`；positive-linear raw explicit-contamination canonical shortcut 还缺 `raw_linear_lower_bound` 和 `contamination_dominated`；positive-linear canonical major/minor shortcut 还缺 `combined_lower_bound`、`linear_net_lower_bound` 和 `canonical_contamination_threshold_bound`；positive-linear explicit-contamination canonical major/minor shortcut 还缺 `combined_lower_bound`、`linear_net_lower_bound` 和 `contamination_dominated`；direct DFT positive-linear explicit-contamination shortcut 还缺 `major_arc_linear_lower_bound`、`minor_arc_uniform_dft_bound_valid`、`minor_arc_dft_bound_sq_error_bound` 和 `contamination_dominated`，其中 `minor_arc_uniform_dft_bound_nonneg` 已由平方型 Lean 桥消去；fixed-square-error direct DFT shortcut 进一步只剩 `major_arc_linear_lower_bound`、`minor_arc_uniform_dft_bound_valid` 和 `contamination_dominated` 三个解析义务；最终检查命令记录为 `analytic_handoff_certificate.py --require-complete --check-lean`。
- 字面目标交接：`Gdbh/GeneralHandoff.lean` 现在不仅能从有限证书和阈值覆盖推出 `StrongGoldbach`，还能直接推出 `ExplicitGoldbachLowerBound 100`；`Gdbh/AnalyticBridge.lean` 新增当前 `Certificate100` 专用的 `explicitLowerBound100_from_certificate100_and_explicit_lower_bound`；`analytic_handoff_certificate.py` 生成完整 wrapper 时会在无参数 `StrongGoldbach` theorem 旁边额外生成 `explicitLowerBound100_from_*` theorem，`verify_goldbach.py` 生成的自包含 chunked 有限证书也会导出同类 `ExplicitGoldbachLowerBound 100` 条件桥，避免最终报告只证明等价命题而没有导出核心目标本身。
- 攻坚路线图：`proof_status.json` 顶层 `attack_plan` 和 `analytic_attack_plan.md` 已生成，明确先攻 singular series 与显式 canonical 阈值，再建 raw estimate lemma ledger，最后处理 major/minor arcs、有限证书扩展和最终 handoff；Phase 1 已记录 `Gdbh.goldbachSingularSeriesFromBase_lower_bound`、`Gdbh.goldbachSingularSeriesFromQuarter` 和 `Gdbh.one_fourth_le_goldbachSingularSeriesFromQuarter`，当前证书的 singular-series 下界已能由 Lean 检查，并且 `VonMangoldtQuarterHardyLittlewoodNormalizedCanonicalWeightSumEstimate` 已把 `1/4` 与 `goldbachSingularSeriesFromQuarter` 固定进 canonical HL 入口；raw ledger 现在记录 `rawVonMangoldtGoldbachSum_eq_arithmeticFunction_sum`，把项目 raw Λ 卷积对齐到 `Finset.range n.succ` 上的 `sum_m Λ(m)Λ(n-m)`，并记录 `DiscreteCircleMethod.rawVonMangoldtGoldbachSum_eq_fourier_major_add_minor`，说明任意有限 major/minor 频率分块的 raw decomposition 已可由 Lean 证明；`raw_normalized_error_bound_of_hardy_littlewood_abs_error_bound` 说明显式总绝对误差界可以在 Lean 中转成证书所需的 normalized error 义务；quarter decomposition 路线新增 `total_analytic_error_bound_of_quarter_linear_error_bound`，可把 `majorArcError + minorArcError <= epsilon*n` 和 `epsilon <= relativeError/4` 转成主项相对 total-error 义务；若最终 raw 估计使用不同归一化，必须同时替换 `singularSeries` 字段和下界定理。
- 解析交接证书：显式 weight-sum 示例证书记录 `formalized_obligations = ["singular_series_lower_bound", "non_prime_prime_power_weight_sum_bound"]`，`--check-formalized-lean` 通过；canonical 示例证书合法但未完成，只留下 HL 归一化误差解析义务和最终 `derivedThresholdBound` 交接义务；quarter canonical 示例证书合法但未完成，把 singular-series 字段和下界从证书面移入 Lean 对象，只留下 `goldbachSingularSeriesFromQuarter` 主项下的 `raw_normalized_error_bound` 和最终 `canonicalContaminationThresholdBound`；quarter explicit-contamination canonical 示例证书合法但未完成，把最终阈值覆盖改成两个显式数值阈值自动合成，因此只剩 `raw_normalized_error_bound` 和 `contamination_dominated` 两条 Lean 义务，且 `contamination_dominated` 现在可由 `quarter_canonicalNonPrimePrimePowerVonMangoldtWeightSumBound_contamination_dominated_of_sqrt_log_model_ge_two_threshold` 从阈值至少 2 和显式 sqrt-log 模型界推出；quarter lower-bound explicit-contamination 示例证书合法但未完成，只剩 `raw_relative_lower_bound` 和 `contamination_dominated` 两条义务，也可由可选 `contamination_sqrt_log_model_bound` 导出污染支配；positive-linear raw canonical 示例证书合法但未完成，把估计义务压成 `raw_linear_lower_bound`，即逐点 `coefficient*n <= RawVonMangoldtGoldbachSum n`，最终仍需 `canonicalContaminationThresholdBound` 或 `derivedThresholdBound`；positive-linear raw explicit-contamination canonical 示例证书合法但未完成，把最终阈值覆盖改成两个显式数值阈值自动合成，因此只剩 `raw_linear_lower_bound` 和 `contamination_dominated` 两条 Lean 义务；positive-linear canonical major/minor 示例证书合法但未完成，把估计义务压成 `combined_lower_bound` 与 `linear_net_lower_bound`，即逐点证明 major/minor 合成 raw 下界和扣除误差后的正线性 net 下界，最终仍需 `canonicalContaminationThresholdBound` 或 `derivedThresholdBound`；positive-linear explicit-contamination canonical major/minor 示例证书合法但未完成，把最终阈值覆盖改成三个显式数值阈值自动合成，因此只剩 `combined_lower_bound`、`linear_net_lower_bound`、`contamination_dominated` 三条 Lean 义务；decomposition 示例证书合法但未完成，最终交接现在可由四个数值解析阈值自动覆盖加 `canonicalContaminationThresholdBound` 污染阈值覆盖组成，仍保留 `derivedThresholdBound` 直接阈值证明作为后备；linear-error decomposition 示例证书同样合法但未完成，把 total-error 义务换成 `total_linear_error_bound` 并由生成 wrapper 检查 `analyticErrorCoefficient <= relativeError/4`；DFT/L2 示例证书合法但未完成，把 major 频率逐项 DFT-square 模型逼近、模型和逼近、minor DFT 点态界、minor L2 平方和界与 total linear error 直接接到 `DiscreteCircleMethod` 的当前最强 handoff；uniform-minor DFT 示例证书同样合法但未完成，把 minor 部分压成统一 DFT 上界、频率数量上界和一个标量平方误差不等式；uniform-minor-square DFT 示例证书合法但未完成，把频率数量交给 Lean 的平凡上界，只要求统一 DFT 上界和 `M(n)^2 <= minorArcError(n)`；canonical 污染支配已从匿名 eventual 阈值推进到 `canonicalHLContaminationThreshold`、`canonicalHLContaminationThreshold_spec`、`canonicalLinearContaminationThreshold_spec` 和 split-threshold direct raw weight-sum 桥，但该阈值仍需具体上界或有限证书覆盖；canonical 证书即使达到 `estimate_complete: true`，最终仍因缺少最终阈值覆盖证明而不会标记为 `complete: true`；完整证书若提供 `finiteCertificateTheorem`，导出 wrapper 会生成无参数 `StrongGoldbach` theorem；`--check-lean` 会类型检查这个 wrapper，防止错误的最终阈值证明项只通过元数据校验。
- 有限验证：验证每个 `4 <= N <= 100000` 的偶数都有两个素数表示。
- 有限 von Mangoldt 探索：`analyze_von_mangoldt.py 1000 --coefficient 1.0 --relative-error 0.5` 显示旧 doubled count/log 污染预算在样本范围内尚未低于 `1*n`，新的 doubled weight-sum 污染预算从 `n = 184` 起在样本尾部低于 `1*n`，近似 HL 归一化误差从 `n = 32` 起在样本尾部低于 `0.5`，但 canonical HL 证书形状 `(c=1, δ=0.5)` 在样本范围内仍未满足；这只是有限数值探索，不是证明。
- Lean 证书生成：生成 `Gdbh/Certificate100.lean`，证明 `GoldbachUpTo 100`；生成 `Gdbh/Certificate50000.lean`，证明 `Gdbh.goldbachBetween2To50000` 和 `Gdbh.goldbachUpTo50000_of_chunkedCertificate2To50000`。
- 奇异级数局部乘子：Lean 证明 `one_le_goldbachSingularSeriesLocalMultiplier` 和 `goldbachSingularSeriesFromBase_lower_bound`，说明 odd-prime 局部修正因子不会削弱非负 base 常数；当前证书进一步使用 `goldbachSingularSeriesFromQuarter` 和 `one_fourth_le_goldbachSingularSeriesFromQuarter` 关闭 quarter-base singular-series 下界义务。
- 区间证书生成：生成器可导出单区间或 chunked `GoldbachBetween A B` 区间证书，并由 Lean 拼接成更大的 `GoldbachUpTo B`；当前自包含 `2 < n <= 50000` chunked 证书已通过 Lean 检查；从 `2` 开始的 chunked 证书会导出 `ExplicitGoldbachLowerBound 100` 条件桥，并生成自包含 `GoldbachUpTo B`，还对 `ExplicitGoldbachLowerBound`、`CircleMethodLowerBound`、`MajorMinorArcEstimate`、`GoldbachCountPositiveAbove`、`WeightedGoldbachLowerBound`、`WeightedMajorMinorArcEstimate`、`ContaminatedWeightedGoldbachLowerBound`、`ContaminatedWeightedMajorMinorArcEstimate`、`RealContaminatedWeightedGoldbachLowerBound`、`VonMangoldtGoldbachLowerBound`、`VonMangoldtMajorMinorArcEstimate`、`VonMangoldtPrimePowerContaminationLowerBound`、`VonMangoldtSplitPrimePowerContaminationLowerBound`、`VonMangoldtPointwiseSplitContaminationLowerBound`、`VonMangoldtUniformSplitContaminationLowerBound`、`VonMangoldtCountedSplitContaminationLowerBound`、`VonMangoldtTrivialCountSplitContaminationLowerBound`、`VonMangoldtWeightBoundSplitContaminationLowerBound`、`VonMangoldtLogWeightSplitContaminationLowerBound`、`VonMangoldtCountBoundLogWeightSplitContaminationLowerBound`、`VonMangoldtCanonicalLogCountContaminationLowerBound`、`VonMangoldtDirectRawLogCountLowerBound`、`VonMangoldtDirectRawWeightSumLowerBound`、`VonMangoldtSplitThresholdDirectRawWeightSumLowerBound`、`VonMangoldtEventuallyDirectRawWeightSumLowerBound`、`VonMangoldtPositiveLinearRawWeightSumLowerBound`、`VonMangoldtRelativeErrorPositiveLinearRawWeightSumLowerBound`、`VonMangoldtRelativeErrorWeightSumMajorMinorArcEstimate`、`VonMangoldtSplitThresholdRelativeErrorWeightSumMajorMinorArcEstimate`、`VonMangoldtSplitThresholdPositiveLinearRawWeightSumLowerBound`、`VonMangoldtEventuallyPositiveLinearRawWeightSumLowerBound`、`VonMangoldtEventuallyPositiveLinearWeightSumMajorMinorArcEstimate`、`VonMangoldtSplitThresholdPositiveLinearWeightSumMajorMinorArcEstimate`、`VonMangoldtDirectWeightSumMajorMinorArcEstimate`、`VonMangoldtSplitThresholdDirectWeightSumMajorMinorArcEstimate`、`VonMangoldtEventuallyDirectWeightSumMajorMinorArcEstimate` 及其 major/minor arc 变体导出最终 `StrongGoldbach` 条件桥；临时生成的自包含 `2 < n <= 30` chunked 证书已通过 Lean 检查和生成桥接定理公理审计。
- 形式化归约：Lean 证明了强猜想等价于任意 `B ≥ 2` 的有限区间证明加无限区间证明；`Gdbh.Round1Status.binaryGoldbachConjecture_iff_goldbachUpTo_and_explicitLowerBound` 进一步把同一分解直接提升到 `goal.md` 的字面 `BinaryGoldbachConjecture`；`Gdbh.Round1Status.binaryGoldbachConjecture_iff_explicitLowerBound_of_goldbachUpTo_le` 还证明了只要已有 `GoldbachUpTo B` 且 `2 ≤ T ≤ B`，字面目标就等价于 `ExplicitGoldbachLowerBound T`。
- 证书归约：Lean 证明了在当前 `GoldbachUpTo 50000` chunked 证书下，完整强猜想等价于 `Gdbh.ExplicitGoldbachLowerBound 50000`；具体定理是 `Gdbh.Round1Status.binaryGoldbachConjecture_iff_explicitLowerBound50000_of_chunkedCertificate2To50000`；`GoldbachUpTo 100` 仍保留为 legacy 低层证书。
- 计数桥接：Lean 证明了当前 50000 证书下，完整强猜想等价于 `50000` 以上每个偶数的 `GoldbachCount` 为正。
- 精确目标交接：`Gdbh/GoalHandoff.lean` 现在把当前 50000 证书、若干关键解析接口以及 Path A inner-bilinear / inner-pointwise 接口直接桥到 `Gdbh.Round1Status.BinaryGoldbachConjecture`，即 `goal.md` 的字面二元哥德巴赫命题；新增的 exact-goal 桥还覆盖 quarter Hardy-Littlewood normalized canonical、Hardy-Littlewood absolute-error canonical、split-threshold major/minor absolute-error canonical、quarter decomposition canonical、DFT uniform-minor-square quarter linear-error canonical，以及显式污染项 DFT positive-linear frontiers；同一文件还新增直接桥到当前机器剩余目标 `Gdbh.Round1Status.CurrentFormalTarget50000`，让阈值 `T <= 50000` 的 `ExplicitGoldbachLowerBound T`、`GoldbachCountPositiveAbove T` 或选定解析接口可直接关闭 `Gdbh.ExplicitGoldbachLowerBound 50000`；`Gdbh/CounterexampleReduction.lean` 进一步把同一个当前尾目标等价改写为“50000 以上不存在 `GoldbachCount n = 0` 的偶数反例”，并把这个计数反例逐点等价到“50000 以上不存在 `GoldbachRepresentation` 失败”的表示型反例形式，还新增 `BinaryGoldbachCounterexample` 字面反例谓词，证明当前 50000 证书会把任何字面反例推到同一个 `n > 50000` 尾部，并证明若字面目标失败则存在最小的字面反例；现在还新增 `GoldbachHalfWitness` 下半区素数补数形式，证明 `GoldbachRepresentation n` 等价于存在 `p <= n / 2` 且 `p` 与 `n - p` 都为素数，并把当前 50000 尾目标等价到这个 lower-half witness 目标；现在还新增 `MinimalBinaryGoldbachCounterexample` 归纳式 package，记录该最小反例 `m > 50000` 且每个更小的偶数 `n >= 4` 已有 `GoldbachRepresentation`，并把当前 `ExplicitGoldbachLowerBound 50000` 目标等价为“不存在这样的 package”；同一文件进一步证明该最小反例会强制一个 `LowerHalfPrimeComplementDivisorCover`：每个下半区素数 `p` 的补数 `m - p` 都有真素因子，并定义 `MinimalCounterexamplePrimeDivisorCover`，把当前剩余目标等价为不存在这种筛法覆盖 package；同一文件进一步把该真素因子覆盖等价改写为 `LowerHalfPrimeComplementResidueCover`：每个下半区素数 `p` 都满足某个真素因子模数下的 `p ≡ m [MOD r]`，并定义 `MinimalCounterexamplePrimeResidueCover`，把当前剩余目标也等价为不存在这种同余覆盖 package；同一文件进一步定义 `PrimeModuliBelow m` 和 `LowerHalfPrimeComplementFiniteResidueCover`，把同余覆盖精确有限化为只使用小于 `m` 的素数模数，并定义 `MinimalCounterexampleFinitePrimeResidueCover`，把当前剩余目标也等价为不存在这种有限素数模数覆盖 package；同一个有限 cover 现在还有对偶 escape 义务：`LowerHalfPrimeFiniteResidueEscape` / `MinimalCounterexampleFiniteResidueEscape` 要求为任意假设的最小反例找一个避开所有有限坏同余类的下半区素数，Lean 已证明这种 escape 会强制补数为素数、给出 `GoldbachHalfWitness` 并关闭当前目标；现在还新增有限基数充分条件 `LowerHalfPrimeResidueSurplus` / `MinimalCounterexampleFiniteResidueSurplus`，把这个 escape 交接压成证明 `LowerHalfPrimes m` 的基数严格大于有限坏同余覆盖 `BadLowerHalfPrimes m` 的基数；进一步新增 union-bound 充分条件 `BadLowerHalfPrimeResidueClassCardSum` / `MinimalCounterexampleFiniteResidueUnionBoundSurplus`，把有限并集基数上界替换为各坏同余类基数之和；这些仍只是充分的筛法交接义务；同一文件进一步定义 `MinimalBinaryCounterexampleDescent`，证明若能从任意归纳式最小反例构造更小字面反例，就可推出字面目标和当前 `ExplicitGoldbachLowerBound 50000`，并证明该下降义务本身与当前剩余目标及 lower-half witness 目标等价；这些都是交接或反例归约定理，不证明缺失的解析估计。
- 解析接口：Lean 证明了 `ExplicitGoldbachLowerBound 50000` 是当前最强有限归约目标，并可推出 legacy `ExplicitGoldbachLowerBound 100` 与 `StrongGoldbach`。
- 两段筛法交接：`Gdbh/CounterexampleReduction.lean` 现在还记录 `MinimalCounterexampleFiniteResidueSplitSurplus` 和 `MinimalCounterexampleFiniteResidueSmallUnionBoundSplitSurplus`，允许未来证明为每个最小反例选择阈值 `R`，只对小模数坏同余类使用 union-bound，并把大模数覆盖集保留为单独的精确有限目标；同一文件进一步新增 `LargeBadLowerHalfPrimeCofactorPairs` 和 `MinimalCounterexampleFiniteResidueSmallUnionBoundCofactorSplitSurplus`，把大模数覆盖集上界下沉到互补数分解 `m - p = r * k` 且 `k >= 2` 的有限 cofactor pair 计数；`LargeBadLowerHalfPrimeBoundedCofactorPairs` / `MinimalCounterexampleFiniteResidueSmallUnionBoundBoundedCofactorSplitSurplus` 又利用 `r > R` 把同一 witness 空间收紧到 `k <= m / (R + 1)`；`LargeBadLowerHalfPrimeCofactorBox` / `MinimalCounterexampleFiniteResidueSmallUnionBoundCofactorBoxSplitSurplus` 将 bounded-pair 计数进一步上界为显式矩形大小 `#PrimeModuliBelowAbove m R * #Icc 2 (m / (R + 1))`；cofactor-box product 版本把区间基数化简为纯自然数因子 `m / (R + 1) - 1`，并通过 `explicitLowerBound50000_of_minimalCounterexampleFiniteResidueCofactorBoxProductSplitSurplus` 交接到当前目标；prime-counting window 版本在 `R < m` 下把大模数个数改写为 `Nat.primeCounting (m - 1) - Nat.primeCounting R`，并通过 `explicitLowerBound50000_of_minimalCounterexampleFiniteResidueCofactorPrimeCountingSplitSurplus` 交接到当前目标；full prime-counting 版本还把下半区素数目标基数改写为 `Nat.primeCounting (m / 2)`，并通过 `explicitLowerBound50000_of_minimalCounterexampleFiniteResidueCofactorPrimeCountingFullSplitSurplus` 交接到当前目标；point-bound full prime-counting 版本进一步证明任意固定剩余类在 `0 <= p <= m / 2` 中至多有 `m / 2 / r + 1` 个点，把小模数坏剩余类和上界到 `BadLowerHalfPrimeSmallResidueClassPointBoundSum m R`，并通过 `explicitLowerBound50000_of_minimalCounterexampleFiniteResidueSmallPointBoundCofactorPrimeCountingFullSplitSurplus` 交接到当前目标；最新 catch 证明该 point-bound 版本过粗：`not_lowerHalfPrimeResidueSmallPointBoundCofactorPrimeCountingFullSplitSurplus_of_eight_le` 表明任意 `m >= 8`、任意 `R` 都不满足这个局部不等式，`minimalCounterexampleFiniteResidueSmallPointBoundCofactorPrimeCountingFullSplitSurplus_iff_no_minimalBinaryCounterexample` 则说明对应的最小反例义务等价于“不存在最小二元反例”，不是可在真实大反例上验证的筛法不等式；parity-aware point-bound 版本把模数 `2` 从点界和式中剥离出来，用一点评界处理它，并通过 `explicitLowerBound50000_of_minimalCounterexampleFiniteResidueSmallParityPointBoundCofactorPrimeCountingFullSplitSurplus` 交接到当前目标，但新的 catch `not_lowerHalfPrimeResidueSmallParityPointBoundCofactorPrimeCountingFullSplitSurplus_of_eight_le` 证明这条修复也过强；Lean 已证明这些充分义务会关闭当前 `ExplicitGoldbachLowerBound 50000` 目标，但点界类义务本身已经被证明不能作为真实大反例上的筛法不等式使用。
- Point-bound 精确死端：同一文件进一步新增
  `Gdbh.CounterexampleReduction.minimalCounterexampleFiniteResidueSmallPointBoundCofactorPrimeCountingFullSplitSurplus_iff_explicitLowerBound50000`
  和
  `Gdbh.CounterexampleReduction.minimalCounterexampleFiniteResidueSmallParityPointBoundCofactorPrimeCountingFullSplitSurplus_iff_explicitLowerBound50000`，
  以及对应的 `..._iff_currentFormalTarget50000`、`..._iff_halfWitnessLowerBound50000`
  形式，明确两个点界类最小反例义务都直接等价于当前 `ExplicitGoldbachLowerBound 50000` 尾目标、状态层目标和 lower-half witness 尾目标；因此它们不是可独立关闭的真实大反例筛法不等式。
- Descent 精确死端：`MinimalBinaryCounterexampleDescent` 现在也被证明等价于“不存在 induction-ready 最小二元反例”、当前状态层目标、当前 `ExplicitGoldbachLowerBound 50000` 尾目标，以及 50000 以上不存在 lower-half witness 反例；因此它记录的是最小反例证明的逻辑形状，不是比当前尾目标更弱的待证义务。
- Universal handoff 精确强度：`minimalCounterexampleObligation_iff_no_minimalBinaryCounterexample` 和 `minimalCounterexampleObligation_iff_explicitLowerBound50000` 把所有能关闭目标的 universal minimal-counterexample handoff 归为 exact-strength 接口；新增 wrapper 尤其记录了 `MinimalCounterexampleFiniteResidueSmallUnionBoundCofactorPrimeCountingFullSplitSurplus` 本身也直接等价于当前尾目标。
- Cofactor image 精确性：Lean 现在证明了 `largeBadLowerHalfPrimeCofactorImage_subset_largeModuli`，并与既有的 `badLowerHalfPrimesLargeModuli_subset_cofactorImage` 合成 `largeBadLowerHalfPrimeCofactorImage_eq_largeModuli` 及 `largeBadLowerHalfPrimeCofactorImage_card_eq_largeModuli_card`；因此 `LargeBadLowerHalfPrimeCofactorImage` 正好是大模数覆盖集，不只是 cofactor-pair 上界前的松散 universe。
- Bounded cofactor 精确性：Lean 现在还证明了 `largeBadLowerHalfPrimeBoundedCofactorPairs_subset_pairs`，结合既有的 `largeBadLowerHalfPrimeCofactorPairs_subset_bounded` 得到 `largeBadLowerHalfPrimeCofactorPairs_eq_bounded` 和 `largeBadLowerHalfPrimeCofactorPairs_card_eq_bounded`；因此 bounded cofactor 版本只是把 ambient range 从 `0..m` 精确缩到 `0..m/(R+1)`，没有改变 witness 集本身。
- Sqrt cofactor 精确性：新增 `Gdbh/CounterexampleSqrtCofactor.lean`，证明若 `m < (R + 1)^2`，则 cofactor pair 投影 `(r,k) ↦ m - r*k` 在 `LargeBadLowerHalfPrimeCofactorPairs m R` 上单射；因此 `largeBadLowerHalfPrimeCofactorImage_card_eq_cofactorPairs_card_of_mul_lt` 和 `badLowerHalfPrimesLargeModuli_card_eq_cofactorPairs_card_of_mul_lt` 给出大模数覆盖集与 cofactor-pair relation 的精确基数等式，并推出 `lowerHalfPrimeResidueSmallUnionBoundSplitSurplus_iff_cofactorSplitSurplus_of_mul_lt`。同一文件现在还用 `square_lt_of_sqrt_le` 证明自然阈值 `Nat.sqrt m <= R` 可直接触发这些结论，并给出对应的 `..._of_sqrt_le` 包装定理；进一步固定 `R = Nat.sqrt m`，记录 `badLowerHalfPrimesLargeModuli_card_eq_cofactorPairs_card_at_sqrt` 和 `lowerHalfPrimeResidueSmallUnionBoundSplitSurplus_iff_cofactorSplitSurplus_at_sqrt`。新的首选固定 sqrt handoff 是精确 cofactor-cardinality 版本 `LowerHalfPrimeResidueSmallUnionBoundSqrtCofactorSplitSurplus`：它避免把大模数项放大成 rectangular prime-counting product，已证明可推出且按通用最小反例诊断等价于当前 `ExplicitGoldbachLowerBound 50000` 尾目标。另有 side-condition-free 的 fixed-sqrt prime-counting/product handoff 作为更粗的算术化界面；现在也拆出了 small-residue 与 box-product component feasibility，并证明相应 component-budget obstruction 与 `ExplicitGoldbachLowerBound 50000` 不相容；更强的 total-budget obstruction 则精确等价于 fixed-sqrt full split 失败，并在 minimal-counterexample 层等价于 `¬ ExplicitGoldbachLowerBound 50000`，同时正向记录 `ExplicitGoldbachLowerBound 50000` 等价于不存在该 exact budget obstruction；借助 50000 证书，该 exact obstruction 现在也直接等价于 literal binary Goldbach statement、当前 `CurrentFormalTarget50000` 和项目 `StrongGoldbach` headline 的否定，并已展开成逐点形式：证明 headline 等价于对每个 `MinimalBinaryGoldbachCounterexample m` 排除局部 exact total-budget obstruction；同一层面现在还把该排除条件改写为完全显式严格不等式 `BadLowerHalfPrimeSmallResidueClassCardSum m (Nat.sqrt m) + SqrtCofactorPrimeCountingFullBoxProductBudget m < Nat.primeCounting (m / 2)`，并记录 `StrongGoldbach` 与这个逐点严格预算目标等价；最新的 `LowerHalfPrimeResidueSmallUnionBoundSqrtCofactorPrimeCountingFullBudgetSplitSurplus` 又把这条严格不等式拆成小 residue 预算、box-product 预算和两预算 margin 三个命名子目标，exact-budget 版本仍与当前状态层目标和 `StrongGoldbach` 等价；同时 Lean 记录 `m = 30` catch：`SqrtCofactorPrimeCountingFullBoxProductBudget 30 = 28` 而 `π(15) = 6`，所以 exact-budget split 在 30 处失败，不能被误用为全局局部不等式。两者都不是解析下界证明，只是把后续义务压成标准平方根切分下的显式不等式。
- Exact cofactor-pair budget split：同一文件现在新增 `SqrtCofactorPairBudgetBound`、`SqrtCofactorPairTwoBudgetMargin` 与 `LowerHalfPrimeResidueSmallUnionBoundSqrtCofactorPairBudgetSplitSurplus`，把固定 sqrt 预算拆成小 residue 预算、真实 cofactor-pair 计数预算和两预算 margin，避免经过 rectangular box-product overcount。exact-budget 版本 `LowerHalfPrimeResidueSmallUnionBoundSqrtCofactorPairExactBudgetSplitSurplus` 已证明与精确 fixed-sqrt cofactor handoff 等价；对应 minimal-counterexample 版本也已与当前 `CurrentFormalTarget50000` 和项目 `StrongGoldbach` headline 等价。这仍不是解析闭合，而是把后续义务保留在真实 cofactor relation 上。
- Exact cofactor-fiber budget split：同一层现在进一步新增 `SqrtCofactorFiberPointwiseBudget` 与 `SqrtCofactorFiberPointwiseBudgetSum`，把 exact cofactor-pair budget 拆成逐个 cofactor `k` 的 fiber 预算加总预算；`sqrtCofactorPairBudgetBound_of_fiberPointwiseBudget` 和 `sqrtCofactorPairBudgetSplitSurplus_of_fiberBudgetSplitSurplus` 证明该 fiber-budget split 推出 pair-budget split。fully explicit 版本 `LowerHalfPrimeResidueSmallUnionBoundSqrtCofactorFiberExactBudgetSplitSurplus` 已证明与 exact pair-budget split 等价，并在 minimal-counterexample 层等价于 `CurrentFormalTarget50000` 与 `StrongGoldbach`。同一层现在还拆出必要条件 `LowerHalfPrimeResidueSmallUnionBoundSqrtCofactorFiberComponentFeasibility`：exact small-residue budget 和 exact fiber-sum budget 必须各自严格小于 `Nat.primeCounting (m / 2)`；disjunctive `...ComponentBudgetObstruction` 已证明正好等价于 component feasibility 失败，并且在 minimal-counterexample 层与 `CurrentFormalTarget50000`、`ExplicitGoldbachLowerBound 50000`、`StrongGoldbach` 不相容。最新的 total-budget obstruction `LowerHalfPrimeResidueSmallUnionBoundSqrtCofactorFiberBudgetObstruction` 把两项相加，已证明它正好等价于 exact fiber split 失败；minimal-counterexample 版本等价于 `¬ CurrentFormalTarget50000`、`¬ ExplicitGoldbachLowerBound 50000`、`¬ StrongGoldbach`，正向则改写成每个 minimal counterexample 上的严格不等式 `LowerHalfPrimeSmallResidueClassSqrtExactBudget m + SqrtCofactorFiberExactBudgetSum m < Nat.primeCounting (m / 2)`；局部 exact fiber split 本身也已直接证明等价于同一个严格两预算不等式。最新补充的 `sqrtCofactorFiberBudgetStrict_iff_fiberSplitSurplus`、`sqrtCofactorFiberExactBudgetSplitSurplus_iff_fiberSplitSurplus` 和 minimal-counterexample wrapper 还把该 strict budget 形式同早先的 fixed-sqrt fiber-sum cardinality criterion 精确识别为同一个义务。它只是把后续义务拆到更细的 fiber 估计，不是新的解析下界。
- Fixed sqrt cofactor fiber decomposition：同一文件现在定义 `LargeBadLowerHalfPrimeSqrtCofactorFiber m k`，并证明 `largeBadLowerHalfPrimeCofactorPairs_card_eq_sum_fibers_at_sqrt`，把固定平方根切分下的 exact cofactor-pair count 精确分解为 `sum_{2 <= k <= sqrt m} # {r : prime modulus above sqrt m | m - r*k is lower-half prime}`。新增 `LowerHalfPrimeResidueSmallUnionBoundSqrtCofactorFiberSplitSurplus` 与 `lowerHalfPrimeResidueSmallUnionBoundSqrtCofactorFiberSplitSurplus_iff_sqrtCofactorSplitSurplus`，说明 fiber-sum 版不是更强的 overcount，而是精确改写；对应最小反例 wrapper 同样等价于当前 `ExplicitGoldbachLowerBound 50000` 尾目标。
- Fixed sqrt cofactor-residue 上界：同一文件进一步定义 `LowerHalfPrimeCofactorResidueClass m k = {p ∈ LowerHalfPrimes m | p ≡ m [MOD k]}`，证明每个 `LargeBadLowerHalfPrimeSqrtCofactorFiber m k` 经 `r ↦ m - r*k` 注入该 residue class，并得到 `largeBadLowerHalfPrimeCofactorPairs_card_le_sum_cofactorResidueClasses_at_sqrt`。新增 `LowerHalfPrimeResidueSmallUnionBoundSqrtCofactorResidueSplitSurplus` 及 wrapper，作为比 box 更细、但比 exact fiber-sum 更容易衔接同余类估计的固定平方根 handoff；它仍按通用最小反例诊断等价于当前尾目标，不是解析闭合。
- Coprime cofactor-residue 拆分：同一接口现在把 `LowerHalfPrimeCofactorResidueClass m k` 分成 `Nat.Coprime p k` 与非互素两部分；非互素部分被证明注入统一异常集 `LowerHalfPrimeDivisors m`，因为 `p ∣ k` 且 `p ≡ m [MOD k]` 推出 `p ∣ m`。新增 `LowerHalfPrimeResidueSmallUnionBoundSqrtCofactorCoprimeResidueSplitSurplus`，把后续大模数义务压到 coprime residue classes 加 divisor-error budget，并仍按最小反例诊断等价于当前 `ExplicitGoldbachLowerBound 50000` 尾目标。
- Divisor-error prime-counting 化：`LowerHalfPrimeDivisors m` 现在按 `p ≤ Nat.sqrt m` 与 `Nat.sqrt m < p` 拆分；小部分注入 `PrimeModuliBelowAtMost m (Nat.sqrt m)`，大于平方根的素因子至多一个。因此对 `m > 1` 有 `lowerHalfPrimeDivisors_card_le_primeCounting_sqrt_add_one_of_one_lt : #LowerHalfPrimeDivisors m ≤ π(√m)+1`。新增 `LowerHalfPrimeResidueSmallUnionBoundSqrtCofactorCoprimeResiduePrimeCountingDivisorSplitSurplus`，把 divisor-error budget 从重复的实际 divisor 集合进一步替换为显式 `(√m - 1) * (π(√m)+1)`；该 handoff 仍是尾目标等价诊断，不是解析闭合。
- Coprime cofactor-modulus 支撑缩减：同一固定平方根接口现在证明若 `p ∈ LowerHalfPrimeCofactorResidueClassCoprime m k`，则 `Nat.Coprime m k`；因此非互素模数 `k` 的 coprime residue class 为空，`sum_{2 <= k <= sqrt m}` 可精确缩到 `CoprimeCofactorsAtSqrt m`。新增 `LowerHalfPrimeResidueSmallUnionBoundSqrtCofactorCoprimeModulusPrimeCountingDivisorSplitSurplus`，把后续解析义务限制在与 `m` 互素的 cofactor 模数上，并保留同一个显式 divisor-error budget；该 handoff 仍只等价诊断当前尾目标，不是解析闭合。
- Primitive residue-class 改写：在上述 `Nat.Coprime m k` 支撑上，Lean 又证明 `p ≡ m [MOD k]` 自动推出 `Nat.Coprime p k`，所以 `LowerHalfPrimeCofactorResidueClassCoprime m k = LowerHalfPrimeCofactorResidueClass m k`。新增 `LowerHalfPrimeResidueSmallUnionBoundSqrtCofactorPrimitiveResiduePrimeCountingDivisorSplitSurplus`，把后续解析目标表述为对 primitive cofactor moduli 的普通 residue class `p ≡ m [MOD k]` 求和，再加同一个显式 divisor-error budget；这只是把估计接口规范化为标准 primitive AP 形式。
- AP prime-counting set 标准化：同一文件现在定义 `PrimeResidueClassUpTo N a q = {p ≤ N | p.Prime ∧ p ≡ a [MOD q]}`，并证明 `LowerHalfPrimeCofactorResidueClass m k = PrimeResidueClassUpTo (m / 2) m k`。新增 `LowerHalfPrimeResidueSmallUnionBoundSqrtCofactorAPResiduePrimeCountingDivisorSplitSurplus`，把 primitive cofactor-residue handoff 的 summand 改写为显式 AP prime-counting set；该接口仍等价诊断当前尾目标，不含新的素数分布估计。
- AP full-RHS 标准化：同一接口进一步新增 `LowerHalfPrimeResidueSmallUnionBoundSqrtCofactorAPResiduePrimeCountingDivisorFullSplitSurplus`，把右侧 `(LowerHalfPrimes m).card` 精确改写为 `Nat.primeCounting (m / 2)`，并证明 `...SplitSurplus_iff_apResiduePrimeCountingDivisorFullSplitSurplus`。这让剩余义务完全呈现为 `BadLowerHalfPrimeSmallResidueClassCardSum`、primitive cofactor AP prime-counting sum、显式 divisor-error budget 与 `π(m/2)` 的单个自然数不等式；仍然没有加入新的解析估计。
- AP budget split：同一 full-RHS 接口现在拆成两个命名子目标：`LowerHalfPrimeSmallResidueClassSqrtBudget m budget` 控制小 residue-class 贡献，`SqrtCofactorAPResiduePrimeCountingDivisorRemainderBudget m budget` 控制 primitive cofactor AP sum 加 divisor-error 后仍低于 `π(m/2)`。`LowerHalfPrimeResidueSmallUnionBoundSqrtCofactorAPResiduePrimeCountingDivisorBudgetSplitSurplus` 将两者合并，且已证明可推出 full-RHS handoff；这是后续解析估计的预算分配接口，不是闭合证明。
- AP three-budget split：预算接口进一步拆成 `SqrtCofactorAPResidueClassSumBudget`、`SqrtCofactorPrimeCountingDivisorErrorBudget` 和纯算术 margin `SqrtCofactorAPResiduePrimeCountingDivisorThreeBudgetMargin`。新增 `LowerHalfPrimeResidueSmallUnionBoundSqrtCofactorAPResiduePrimeCountingDivisorThreeBudgetSplitSurplus`，把小 residue 项、primitive AP sum、divisor-error 项三块分别命名；Lean 已证明三预算 handoff 可推出上一层 one-budget handoff，并保留最小反例等价诊断。
- AP pointwise-budget split：primitive AP sum 预算现在又拆成逐模数预算 `SqrtCofactorAPResidueClassPointwiseBudget m pointBudget` 与汇总预算 `SqrtCofactorAPResidueClassPointwiseBudgetSum m pointBudget apBudget`。Lean 证明 `sqrtCofactorAPResidueClassSumBudget_of_pointwiseBudget`，所以新的 `LowerHalfPrimeResidueSmallUnionBoundSqrtCofactorAPResiduePrimeCountingDivisorPointwiseAPBudgetSplitSurplus` 可推出 three-budget handoff；后续解析义务可逐个 cofactor modulus 估计 `#PrimeResidueClassUpTo (m/2) m k`。
- AP elementary point-budget split：逐模数 AP 预算又有一个完全初等的专门化。Lean 证明 `PrimeResidueClassUpTo N a q` 的基数至多 `N / q + 1`，于是可固定 `SqrtCofactorAPResidueClassElementaryPointBudget m k = (m / 2) / k + 1`，并用 `SqrtCofactorAPResidueClassElementaryPointBudgetSum m` 汇总。新的 `LowerHalfPrimeResidueSmallUnionBoundSqrtCofactorAPResiduePrimeCountingDivisorElementaryPointAPSplitSurplus` 推出 pointwise AP handoff，并保留到当前尾目标的 exact-strength 诊断；这是显式记录初等损失的接口，不是闭合证明。
- AP fully explicit elementary point split：上一层接口现在进一步固定小 residue 预算为 `BadLowerHalfPrimeSmallResidueClassCardSum m (Nat.sqrt m)`，固定 divisor-error 预算为 `(Nat.sqrt m - 1) * (Nat.primeCounting (Nat.sqrt m) + 1)`。新增 `LowerHalfPrimeResidueSmallUnionBoundSqrtCofactorAPResiduePrimeCountingDivisorElementaryPointAPExplicitSplitSurplus`，把 fixed-sqrt/AP/elementary-point handoff 收成一个无预算函数参数的自然数不等式；Lean 证明它推出 elementary point AP split，并保留 exact-strength 诊断到 `ExplicitGoldbachLowerBound 50000`。
- AP explicit component feasibility：fully explicit split 现在进一步拆出必要条件 `SqrtCofactorAPResidueClassElementaryPointBudgetSumFeasible` 与 `SqrtCofactorPrimeCountingDivisorExactBudgetFeasible`。Lean 证明 explicit split 推出二者，并证明若 summed elementary AP point budget 或 exact divisor-error budget 已经达到 `Nat.primeCounting (m / 2)`，则 local explicit split 不可能成立；同一 obstruction 也提升到 minimal-counterexample 层。新增的 disjunctive component-budget obstruction 把这两个失败条件合并成一个命名残差，并证明它正好等价于 component feasibility 的失败；对应 minimal-counterexample obstruction 也等价于“存在 minimal counterexample 违反 component feasibility”，并且已接到目标层，证明该 obstruction 与 `ExplicitGoldbachLowerBound 50000` 不相容。
- Fixed sqrt cofactor box：同一文件现在还证明 `div_succ_sqrt_le_sqrt`，从既有 intrinsic cofactor bound 推出固定平方根切分下每个 cofactor witness 都满足 `k <= Nat.sqrt m`，并定义 `LargeBadLowerHalfPrimeSqrtCofactorBox`。新增 `largeBadLowerHalfPrimeCofactorPairs_card_le_sqrtCofactorBoxProduct` 和 `lowerHalfPrimeResidueSmallUnionBoundSqrtCofactorSplitSurplus_of_sqrtCofactorBoxSplitSurplus`，把精确 cofactor-cardinality 义务进一步分解为 side-condition-free 的矩形盒目标 `#PrimeModuliBelowAbove m (sqrt m) * #Icc 2 (sqrt m)`；该 box handoff 同样记录了关闭当前 `ExplicitGoldbachLowerBound 50000` 尾目标的 wrapper 和等价诊断。
- Cofactor / bounded-cofactor 义务等价：基于上述基数等式，Lean 现在证明了 `lowerHalfPrimeResidueSmallUnionBoundCofactorSplitSurplus_iff_boundedCofactorSplitSurplus` 与 `minimalCounterexampleFiniteResidueCofactorSplitSurplus_iff_boundedCofactorSplitSurplus`；因此 bounded-cofactor handoff 不再只是单向更强条件，而是与原 cofactor-pair handoff 完全等价。
- Cofactor-box 与 prime-counting 精确改写：Lean 现在还证明 cofactor-box product 只是 `#Icc 2 (m / (R + 1)) = m / (R + 1) - 1` 的精确化简：`lowerHalfPrimeResidueSmallUnionBoundCofactorBoxSplitSurplus_iff_cofactorBoxProductSplitSurplus` 与 `minimalCounterexampleFiniteResidueCofactorBoxSplitSurplus_iff_cofactorBoxProductSplitSurplus`；同理 full prime-counting 只是把 `(LowerHalfPrimes m).card` 精确改写为 `Nat.primeCounting (m / 2)`：`lowerHalfPrimeResidueSmallUnionBoundCofactorPrimeCountingSplitSurplus_iff_cofactorPrimeCountingFullSplitSurplus` 与 `minimalCounterexampleFiniteResidueCofactorPrimeCountingSplitSurplus_iff_cofactorPrimeCountingFullSplitSurplus`。
- Prime-counting 侧条件显式化：Lean 现在定义 `MinimalCounterexampleFiniteResidueSmallUnionBoundCofactorBoxProductSplitSurplusWithLt`，把 prime-counting window 前所需的 `R < m` 从定义内部显式提出，并证明 `lowerHalfPrimeResidueSmallUnionBoundCofactorPrimeCountingSplitSurplus_iff_lt_and_cofactorBoxProductSplitSurplus` 与 `minimalCounterexampleFiniteResidueCofactorPrimeCountingSplitSurplus_iff_cofactorBoxProductSplitSurplusWithLt`；因此 prime-counting window handoff 的精确内容是 cofactor-box-product handoff 加上阈值侧条件。
- 条件接口：Lean 证明了任何能推出 `ExplicitGoldbachLowerBound 50000` 且阈值被 50000 证书覆盖的条件证明程序都可推出 `StrongGoldbach`，但没有证明这些条件假设。
- 圆法接口：Lean 证明了若有具体 `CircleMethodLowerBound`，且阈值不超过 100，则可推出 `StrongGoldbach`。
- Major/minor arc 接口：Lean 证明了若有具体 `MajorMinorArcEstimate`，且阈值不超过 100，则可推出 `StrongGoldbach`。
- 加权表示数接口：Lean 证明了若有具体 `WeightedGoldbachLowerBound`，且阈值不超过 100，则可推出 `StrongGoldbach`。
- 素数指示权重：Lean 证明了 `primeIndicatorWeight` 的带权目标与 `GoldbachCount` 目标等价；公理审计脚本也已覆盖长定理名导致的换行输出。
- 带权 major/minor arc 接口：Lean 证明了若有具体 `WeightedMajorMinorArcEstimate`，且阈值不超过 100，则可推出 `StrongGoldbach`。
- 污染项扣除接口：Lean 证明了若有具体 `ContaminatedWeightedGoldbachLowerBound` 或 `ContaminatedWeightedMajorMinorArcEstimate`，且阈值不超过有限证书上界，则可推出 `StrongGoldbach`；这覆盖了 von Mangoldt/素数幂权重需要额外扣除非素数对贡献的证明形态。
- 实数污染项扣除接口：Lean 证明了若有具体 `RealContaminatedWeightedGoldbachLowerBound` 或 `RealContaminatedWeightedMajorMinorArcEstimate`，且权重非负、阈值不超过有限证书上界，则可推出 `StrongGoldbach`；这更贴近包含 `log p` 的 von Mangoldt 型解析估计。
- Von Mangoldt 专用接口：Lean 证明了 mathlib `Λ` 权重非负，证明 `NonPrimePairVonMangoldtGoldbachSum n = PrimePowerContaminationVonMangoldtGoldbachSum n`，证明素数幂污染项不超过左右两侧污染项之和，证明左右污染项可由逐点项上界加有限和上界控制，证明统一逐项上界乘以污染集合个数上界足以控制污染和，证明左右污染集合个数都不超过 `NonPrimePrimePowerCount n`，证明 `NonPrimePrimePowerCount n <= n + 1`，证明更强的 `NonPrimePrimePowerCount n <= (sqrt n + 1)*(log₂ n + 1)`，定义 `NonPrimePrimePowerVonMangoldtWeightSum` 并证明 `PrimePowerContaminationVonMangoldtGoldbachSum n <= 2 * (NonPrimePrimePowerVonMangoldtWeightSum n * log n)`，还证明 `NonPrimePrimePowerVonMangoldtWeightSum n <= NonPrimePrimePowerCount n * log n` 及其 sqrt-log 计数上界版本，因此旧的 `VonMangoldtSqrtLogCountRawLowerBound` 可自动转换为新的 `VonMangoldtDirectRawWeightSumLowerBound`，正线性 raw Λ、relative-error raw Λ、relative-error major/minor arc、eventually raw Λ 和 eventually major/minor arc 下界也可直接推出 weight-sum 终局桥，并新增 direct raw、split-threshold direct raw、eventually direct raw、positive-linear raw weight-sum、relative-error positive-linear raw weight-sum、eventually relative-error positive-linear raw、eventually abs-error positive-linear raw、relative-error weight-sum major/minor、split-threshold relative-error weight-sum major/minor、eventually relative-error weight-sum major/minor、eventually relative-error sqrt-log count major/minor、split-threshold relative-error sqrt-log count major/minor、split-threshold positive-linear raw weight-sum、eventually positive-linear raw weight-sum、eventually positive-linear weight-sum major/minor、split-threshold positive-linear weight-sum major/minor、direct major/minor、split-threshold direct major/minor 和 eventually direct weight-sum 接口；证明实变量模型 `sqrt(x)*(log x)^3 = o(x)`，并证明精确自然数污染预算最终小于任意正线性函数 `c*n`，证明两个 `Λ` 因子各自有上界时其乘积由上界平方控制，并用 mathlib 的 `ArithmeticFunction.vonMangoldt_le_log` 证明素数幂上 `Λ(m) <= log n`；若有具体 `VonMangoldtDirectRawWeightSumLowerBound`、`VonMangoldtSplitThresholdDirectRawWeightSumLowerBound`、`VonMangoldtEventuallyDirectRawWeightSumLowerBound`、`VonMangoldtPositiveLinearRawWeightSumLowerBound`、`VonMangoldtRelativeErrorPositiveLinearRawWeightSumLowerBound`、`VonMangoldtEventuallyRelativeErrorPositiveLinearRawLowerBound`、`VonMangoldtEventuallyAbsErrorPositiveLinearRawLowerBound`、`VonMangoldtRelativeErrorWeightSumMajorMinorArcEstimate`、`VonMangoldtSplitThresholdRelativeErrorWeightSumMajorMinorArcEstimate`、`VonMangoldtEventuallyRelativeErrorWeightSumMajorMinorArcEstimate`、`VonMangoldtEventuallyRelativeErrorSqrtLogCountMajorMinorArcEstimate`、`VonMangoldtSplitThresholdRelativeErrorSqrtLogCountMajorMinorArcEstimate`、`VonMangoldtSplitThresholdPositiveLinearRawWeightSumLowerBound`、`VonMangoldtEventuallyPositiveLinearRawWeightSumLowerBound`、`VonMangoldtEventuallyPositiveLinearWeightSumMajorMinorArcEstimate`、`VonMangoldtSplitThresholdPositiveLinearWeightSumMajorMinorArcEstimate`、`VonMangoldtDirectWeightSumMajorMinorArcEstimate`、`VonMangoldtSplitThresholdDirectWeightSumMajorMinorArcEstimate`、`VonMangoldtEventuallyDirectWeightSumMajorMinorArcEstimate`、`VonMangoldtSqrtLogCountRawLowerBound`、`VonMangoldtSqrtLogCountLinearRawLowerBound`、`VonMangoldtPositiveLinearRawLowerBound`、`VonMangoldtEventuallyPositiveLinearRawLowerBound`、`VonMangoldtEventuallyPositiveLinearMajorMinorArcEstimate` 或 `VonMangoldtSqrtLogCountMajorMinorArcEstimate`，且阈值不超过有限证书上界，则可推出 `StrongGoldbach`。
- Raw Λ 和记号对齐：Lean 新增 `rawVonMangoldtGoldbachSum_eq_weight_sum` 与 `rawVonMangoldtGoldbachSum_eq_arithmeticFunction_sum`，证明项目的 `RawVonMangoldtGoldbachSum n` 正是 `Finset.range n.succ` 上的 mathlib `ArithmeticFunction.vonMangoldt` 有限卷积。这关闭了 raw estimate ledger 的 R1 记号对齐义务，但不提供任何解析下界。
- little-o raw 入口：Lean 新增 `VonMangoldtEventuallyLittleOAbsErrorPositiveLinearRawLowerBound`，可把 `|Raw-main| = o(main)` 和主项最终正线性转为 abs-error raw 桥。
- asymptotic-equivalent raw 入口：Lean 新增 `VonMangoldtEventuallyAsymptoticEquivalentPositiveLinearRawLowerBound`，可把 `Raw ~ mainTerm` 和主项最终正线性转为 little-o raw 桥。
- normalized-error raw 入口：Lean 新增 `VonMangoldtEventuallyNormalizedErrorPositiveLinearRawLowerBound`，可把 `(Raw-main)/main -> 0` 和主项最终正线性转为 abs-error raw 桥。
- Hardy-Littlewood normalized 入口：Lean 新增 `VonMangoldtEventuallyHardyLittlewoodNormalizedEstimate`，可把 `singularSeries(n) * n` 主项、奇异级数最终正下界和归一化误差趋零转入 normalized-error raw 桥。
- 显式阈值 Hardy-Littlewood normalized 入口：Lean 新增 `VonMangoldtHardyLittlewoodNormalizedEstimate`，可把具体 `threshold` 后的固定归一化误差界转成有效正线性 raw Λ 下界。
- Quarter-base canonical Hardy-Littlewood normalized 入口：Lean 新增 `VonMangoldtQuarterHardyLittlewoodNormalizedCanonicalWeightSumEstimate`，固定使用 `goldbachSingularSeriesFromQuarter` 与系数 `1/4`，自动接入 canonical weight-sum 污染扣除和 direct raw weight-sum 阈值桥；这消除了该路线里的 singular-series 下界证书选择，但仍缺主解析 raw normalized error 和最终阈值覆盖。
- Positive-linear raw canonical 入口：Lean 新增 `VonMangoldtPositiveLinearRawCanonicalWeightSumLowerBound` 和 `canonicalLinearContaminationThreshold_spec`，解析侧只需证明逐点 `coefficient * n <= RawVonMangoldtGoldbachSum n`；Lean 自动使用 canonical non-prime-prime-power weight-sum 上界和正线性污染支配阈值，再接入 direct raw weight-sum 桥。它仍缺主解析 raw Λ 正线性下界和最终阈值覆盖。
- Positive-linear major/minor canonical 入口：Lean 新增 `VonMangoldtSplitThresholdPositiveLinearCanonicalWeightSumMajorMinorArcEstimate`，解析侧可直接给出 `mainTerm - majorArcError <= RawVonMangoldtGoldbachSum + minorArcError` 和 `coefficient*n + minorArcError <= mainTerm - majorArcError` 两条显式阈值不等式；Lean 自动消去 minor error、使用 canonical weight-sum 污染预算并组合阈值。这把 minor arc 攻坚面压到正线性净下界形式，但仍不提供 major arc 近似或 minor arc 统一估计本身。
- 显式阈值 Hardy-Littlewood absolute-error 入口：Lean 新增 `raw_normalized_error_bound_of_hardy_littlewood_abs_error_bound`、`VonMangoldtHardyLittlewoodAbsErrorEstimate` 和 `VonMangoldtHardyLittlewoodAbsErrorCanonicalWeightSumEstimate`，可把 major/minor arc 总绝对误差界直接转成证书需要的 normalized error 义务，并可走 canonical weight-sum 终局桥。
- 显式阈值 Hardy-Littlewood major/minor absolute-error 入口：Lean 新增 `raw_abs_error_bound_of_hardy_littlewood_major_minor_abs_error_bound`、`VonMangoldtHardyLittlewoodMajorMinorAbsErrorEstimate`、split-threshold canonical 版本和 `VonMangoldtQuarterSplitThresholdHardyLittlewoodMajorMinorAbsErrorCanonicalWeightSumEstimate`，可把 `|Raw-main| <= majorArcError + minorArcError` 与总误差支配合成为 total absolute error。通用 split 版本保留 singular-series、major/minor、total-error 三个显式阈值；quarter split 版本固定 `goldbachSingularSeriesFromQuarter` 与系数 `1/4`，只保留 major/minor 与 total-error 两个解析阈值。
- 显式阈值 quarter major/minor decomposition 入口：Lean 新增 `raw_abs_error_bound_of_major_minor_decomposition` 和 `VonMangoldtQuarterSplitThresholdHardyLittlewoodMajorMinorDecompositionCanonicalWeightSumEstimate`，可把 `Raw = majorArcContribution + minorArcContribution`、`|majorArcContribution-main| <= majorArcError`、`|minorArcContribution| <= minorArcError` 先用三角不等式合成为 quarter split absolute-error 义务，再进入 canonical weight-sum 终局桥。这更贴近 R2/R3 分别证明 major arc 主项和 minor arc 统一贡献的论文形态，但仍不提供这些解析估计本身。
- 有限 Fourier decomposition 入口：Lean 新增 `DiscreteCircleMethod.zmodDftConvolution_apply`、`DiscreteCircleMethod.zmodConvolution_eq_fourier_sum`、`DiscreteCircleMethod.rawVonMangoldtGoldbachSum_eq_fourier_major_add_minor`、`DiscreteCircleMethod.rawVonMangoldtGoldbachSum_complex_eq_dft_square_sum`、`DiscreteCircleMethod.majorArcComplexApproximationBound_of_dft_square_term_approximation`、`DiscreteCircleMethod.minorArcComplexContributionBound_of_dft_square_term_bound`、`DiscreteCircleMethod.minorArcComplexContributionBound_of_dft_bound_sq`、`DiscreteCircleMethod.minorArcComplexContributionBound_of_dft_bound_sq_of_uniform_bound`、`DiscreteCircleMethod.minorArcComplexContributionBound_of_dft_bound_sum_sq`、`DiscreteCircleMethod.minorArcDftSquareSumBound_of_uniform_bound_and_card_bound`、`DiscreteCircleMethod.VonMangoldtFourierQuarterLinearErrorCanonicalWeightSumEstimate`、`DiscreteCircleMethod.VonMangoldtFourierComplexQuarterLinearErrorCanonicalWeightSumEstimate`、`DiscreteCircleMethod.VonMangoldtDftModelL2MinorQuarterLinearErrorCanonicalWeightSumEstimate`、`DiscreteCircleMethod.VonMangoldtDftModelUniformMinorQuarterLinearErrorCanonicalWeightSumEstimate`、`DiscreteCircleMethod.VonMangoldtDftModelUniformMinorSqQuarterLinearErrorCanonicalWeightSumEstimate`、`DiscreteCircleMethod.VonMangoldtDftUniformMinorSqPositiveLinearCanonicalLowerBound` 和 `DiscreteCircleMethod.VonMangoldtDftUniformMinorSqPositiveLinearExplicitContaminationCanonicalLowerBound`，可从任意 `majorArcs : (n : Nat) → Finset (ZMod n.succ)` 自动得到 `Raw = Major + Minor`。复数版本允许解析侧证明 `‖MajorComplex - main‖` 与 `‖MinorComplex‖` 的范数界，Lean 用 `Complex.abs_re_le_norm` 转成实部界；major arc 可逐项把显式 DFT 平方 Fourier 项近似到模型项，并把模型和误差接到 `goldbachSingularSeriesFromQuarter(n) * n`，也可在正线性路线中直接证明 `Major >= coefficient*n + minorError`；minor arc 也可先逐项控制显式 DFT 平方 Fourier 项再求和得到复贡献界，或直接给出 `‖S(k)‖ <= M(n)` 和 `M(n)^2 <= minorArcError(n)`，其中 uniform-minor-square DFT 路线不再需要单独证明 `0 <= M(n)`，或给出 L2 型平方和上界 `(1/(n+1))*sum_minor ‖S(k)‖^2 <= minorArcError(n)` / 频率依赖上界 `(1/(n+1))*sum_minor B(k)^2 <= minorArcError(n)`；uniform-minor 入口进一步把 L2 字段压成 `countBound(n) * |1/(n+1)| * M(n)^2 <= minorArcError(n)`，uniform-minor-square 入口则用平凡频率数上界直接要求 `M(n)^2 <= minorArcError(n)`；显式污染版本把最终阈值拆成 major、minor、contamination 三个数值阈值。这些把 raw decomposition 和 minor L2 汇总 bookkeeping 从解析义务降为 Lean 内部恒等式，但仍不提供 major arc 近似或正线性 major 下界、minor arc 统一估计和显式污染支配估计。
- Canonical weight-sum Hardy-Littlewood 入口：Lean 新增 `VonMangoldtHardyLittlewoodNormalizedCanonicalWeightSumEstimate`，复用显式 HL normalized 条件，自动使用 canonical weight-sum 上界和 eventually 污染支配，转入 eventually direct raw weight-sum 终局桥。
- 完全显式阈值 Hardy-Littlewood weight-sum 入口：Lean 新增 `VonMangoldtHardyLittlewoodNormalizedWeightSumEstimate`，可把具体 `threshold` 后的主项估计、prime-power weight-sum 上界和污染预算支配条件直接转入 direct raw weight-sum 终局桥。
- Canonical weight-sum 证书义务：Lean 固定 `canonicalNonPrimePrimePowerVonMangoldtWeightSumBound`，并证明 `canonicalNonPrimePrimePowerVonMangoldtWeightSumBound_valid` 可满足完全显式 HL weight-sum 证书中的 `NonPrimePrimePowerVonMangoldtWeightSum n <= weightSumBound n` 局部义务。
- Canonical 污染预算 eventually 支配：Lean 证明该 canonical weight-sum 污染预算最终小于任意正线性函数，并特别给出 HL 有效系数 `((1-relativeError)*coefficient)*n` 的 eventually 支配定理；这还没有给出显式阈值。
- 通用阈值拼接：Lean 证明了有限证书上界可以提高到任意 `B`，并与阈值 `≤ B` 的解析下界组合推出 `StrongGoldbach`。
- 证书生成器：`Gdbh/Certificate100.lean` 自动包含 `strongGoldbach_from_certificate100_and_circle_method`。
- Lean 源码审计：`audit_lean_source.py` 扫描根模块和 `Gdbh/` 下 Lean 文件，忽略注释/字符串后没有发现 `axiom`、`constant`、`opaque`、`unsafe`、`sorry`、`admit`。
- Lean 公理审计：关键定理的 `#print axioms` 输出只包含允许的 `[propext, Classical.choice, Quot.sound]`。
- 语法检查：通过。
- Lean 安装：通过 `elan` 安装 Lean 4.29.1 和 Lake。
- Lean 构建：`lake build` 成功，只说明形式化框架类型检查通过；不能说明解析桥接定理已被证明。

## 未完成项

新增的 `Gdbh.strongGoldbach_under_RH_unconditional` 是无 RH 的 Path A 包装定理：如果提供 `PathA_UnconditionalOpenContent`（PNT-with-√log 主项、Page-Siegel/Siegel-Walfisz 非主项、T3 aggregation、Vinogradov/Vaughan Type I/II/III 和最终 smallness）以及有限阈值覆盖，则 Lean 可推出 `StrongGoldbach`。这只是 axiom-clean 交接接口，不是无条件证明；这些 analytic content 字段仍是未完成数学目标。

新增的 `Gdbh.strongGoldbach_under_RH_unconditional_refined` 进一步把前两个字段下沉为 `PathA_UnconditionalRefinedOpenContent`：PNT 主项由 `ZetaZeroFreeRegionForPNT` 与 `ZetaZeroFreeRegionToPNTRemainderBridge` 给出，非主项 Siegel-Walfisz 由 `PageSiegelZeroFreeRegion` 与 `PageSiegelToSiegelWalfiszNonPrincipalBridge` 给出。它仍然不是证明；只是把下一阶段 Page-Siegel/PNT 的 mathlib 目标拆成更低层的可替换接口。

新增的 `Gdbh.strongGoldbach_under_RH_unconditional_mathlib_refined` 再把该接口下沉到更贴近 mathlib 的形态：PNT 侧改为从 `ZetaZeroFreeRegionForPNT` 推出 mathlib-native 的 `ChebyshevPsiPNTRemainderBound`，再由 Lean 通过 `pnt_remainder_iff_chebyshev_psi` 转回项目本地 PNT Prop；Page-Siegel 侧改为先证明 `PrimitivePageSiegelZeroFreeRegion`，再通过 `PrimitivePageSiegelToPageSiegelBridge` 处理 imprimitive characters/conductor/Euler factor bookkeeping。它仍然不是证明，但把 Props 2+3 的主攻面拆成更接近未来 mathlib 文件边界的目标。

新增的 `Gdbh.strongGoldbach_under_RH_unconditional_exceptional_pageSiegel` 进一步给出更贴近经典 Page-Siegel/Siegel-Walfisz 证明的入口：解析侧可证明 `PrimitivePageSiegelZeroFreeRegionWithExceptional`，即 primitive characters 在零点区域内至多留下一个可能的实 exceptional zero，然后用 `PrimitivePageSiegelExceptionalToSiegelWalfiszNonPrincipalBridge` 打包 conductor transfer、explicit formula、character orthogonality 和 Siegel lower-bound 对 exceptional contribution 的控制。这避免把 Page-Siegel 主攻面误写成“所有非主 characters 统一无 exceptional zero”的过强目标。

新增的 `Gdbh.strongGoldbach_under_RH_unconditional_exceptional_character_pageSiegel` 把上一条再细化到 optional exceptional primitive character：`PrimitivePageSiegelZeroFreeRegionWithExceptionalCharacter` 不只记录 exceptional modulus 和实零点位置，还记录可能的 primitive non-principal character 本身；Lean 证明 `primitivePageSiegelZeroFreeRegionWithExceptional_of_character` 可把这个精化接口遗忘回旧的 modulus-only exceptional 接口。这更贴近后续 explicit formula 中按 character 分离 exceptional contribution 的实际形式。

新增的 `Gdbh.strongGoldbach_under_RH_unconditional_exceptional_zero_pageSiegel` 继续把 exceptional datum 精化为 `PrimitivePageSiegelExceptionalZero`：除 primitive non-principal character 和 `β` 的区间信息外，还记录 `DirichletLFunction χ (β : ℂ) = 0`。Lean 同时证明 `primitivePageSiegelZeroFreeRegionWithExceptionalCharacter_of_zero` 和 `primitivePageSiegelZeroFreeRegionWithExceptional_of_zero`，因此最精细的 exceptional-zero 接口可自动回退到 character-only 和 modulus-only 两种旧接口。

`PrimitivePageSiegelExceptionalZero.beta_ne_one` 和 `PrimitivePageSiegelExceptionalZero.beta_lt_one` 现在用 mathlib 的 `L(1,χ) ≠ 0` 直接排除 exceptional zero 位于 `1`，把记录的 `β ≤ 1` 强化为 `β < 1`。`primitivePageSiegelExceptionalZero_classifies_region_zeros` 还抽出 Page-Siegel 区域内零点的分类形式：给定 exceptional-zero zero-free-region 语句后，任何实际满足 `DirichletLFunction χ s = 0` 的区域内零点都必须匹配同一个 optional exceptional zero。若未来证明的是更强的无例外 primitive Page-Siegel，`primitivePageSiegelZeroFreeRegionWithExceptionalZero_of_primitivePageSiegel` 等遗忘桥可自动把它接入 exceptional-zero 路线。

新增的 `Gdbh.strongGoldbach_under_RH_unconditional_exceptional_quadratic_zero_pageSiegel` 把 Page-Siegel exceptional datum 再收紧到 classical 形态：`PrimitivePageSiegelExceptionalQuadraticZero` 记录 primitive non-principal character、实际零点 `β`、`L(β,χ)=0`，并额外要求 `χ ^ 2 = 1`。Lean 证明 `primitivePageSiegelZeroFreeRegionWithExceptionalZero_of_quadraticZero` 可把 quadratic-zero 版本遗忘回现有 exceptional-zero 路线，同时 `PrimitivePageSiegelExceptionalQuadraticZero.beta_lt_one` 继承 `L(1,χ) ≠ 0` 排除端点。这更贴近后续 Page/Siegel 证明中“最多一个实二次 exceptional character”的实际定理形态。

新增的 `Gdbh.strongGoldbach_under_RH_unconditional_realChebyshevPNT_exceptional_quadratic_zero_pageSiegel` 把 PNT 侧也下沉到更贴近 mathlib 的实变量形态：解析侧可证明 `ZetaZeroFreeRegionToChebyshevPsiRealPNTRemainderBridge`，即从 zeta 零点自由区域推出 `∀ x ≥ 2` 的 `Chebyshev.psi x - x` 的 `exp(-c * sqrt(log x))` 余项；Lean 通过 `chebyshevPsiPNTRemainderBound_of_real` 自动专门化到自然数点，再进入现有 PNT/Siegel-Walfisz 主项路线。这条 wrapper 同时使用最细的 quadratic exceptional-zero Page-Siegel 接口。

新增的 `Gdbh.strongGoldbach_under_RH_unconditional_realChebyshevPNT_exceptional_quadratic_zero_pageSiegel_fareyWitnessT3` 进一步弱化 T3 侧的开放接口：`PathA_T3FareyWitnessAggregation` 只要求对给定 Siegel-Walfisz、per-arc-to-Fourier aggregation 和 Vinogradov Type I/II/III 输入产出一个存在性的 `PathA_HardyLittlewoodSmallnessForFareyWitness`，而不是旧 `PathA_T3Aggregation` 要求的“任意 Farey witness 都成立”的 `PathA_HardyLittlewoodSmallness_witness_form`。Lean 证明 `PathA_T3FareyWitnessAggregation_of_T3Aggregation` 可从旧强接口回退到新接口，并证明 `pathA_quantitativeContent_of_T3FareyWitnessAggregation` 可直接生成最终 Hardy-Littlewood quantitative content。这更贴近最终 circle-method 聚合证明实际会给出的对象。

新增的 `Gdbh.strongGoldbach_under_RH_unconditional_realChebyshevPNT_exceptional_quadratic_zero_pageSiegel_swT3Smallness` 把 T3 侧再下沉一层：`PathA_T3SWSmallnessForFareyWitness` 不再负责组装 Siegel-Walfisz、Fourier aggregation 或 Vinogradov 输入，而是假设这些已分别产出 SW 形状的 Farey major-arc witness 和 `MinorArcCosineSumBound`，只要求证明同一个 Farey witness 上的 major error 与 minor contribution 都相对 `𝔖(n) * n` 足够小。Lean 证明 `PathA_T3FareyWitnessAggregation_of_swSmallness` 可把这个局部小量比较桥提升为 `PathA_T3FareyWitnessAggregation`，并证明 `pathA_quantitativeContent_of_T3SWSmallness` 可直接进入最终 Hardy-Littlewood quantitative content。这把 Prop 4 的剩余任务从“整体 T3 聚合黑盒”缩成了 SW 误差形状和 Vinogradov minor bound 之后的定量阈值/小量比较。

`Gdbh.PathAMinorArc.vinogradovTypeII_cauchySchwarz_innerSum` 现在已形式化 Type II 双线性和的第一步有限和 Cauchy-Schwarz 归约：外层双线性和的平方可由外层系数能量与内层和能量之积控制。进一步新增的 `Gdbh.PathAMinorArc.TypeIIBilinearEnergyEstimateFor` 把 Type II 黑盒下沉为“具体双重和表示 + Cauchy-Schwarz 后能量估计”；`Gdbh.PathAMinorArc.vinogradovTypeIIBoundForVaughanWitnessUnconditional_of_bilinearEnergy` 证明该低层目标一旦给出即可关闭现有无条件 Type II witness bound，`Gdbh.PathAMinorArc.minorArcCosineSumBound_of_vinogradov_typeII_bilinearEnergy` 则把它直接接入 minor-arc 汇总。剩余的 Vinogradov Type II 缺口不再是有限和 Cauchy-Schwarz 或从平方能量回到绝对值的 Lean 胶水，而是证明 Vaughan witness 的双线性表示、对归约后能量的有理逼近、几何和与对数损失估计。

新增的 `Gdbh.PathAMinorArc.VinogradovTypeIIIBilinearEnergyForVaughanWitness` 把 Type III/high-high Vaughan 项也接到同一个双线性能量接口。Lean 证明 `smallPrimesBoundFor_of_bilinearEnergyEstimate` 可把通用能量估计转成第三项使用的 `SmallPrimesBoundFor` 形状，`vinogradovTypeIIIBoundForVaughanWitnessUnconditional_of_bilinearEnergy` 关闭现有 Type III witness bound，`minorArcCosineSumBound_of_vinogradov_bilinearEnergies` 则表明 Type I witness bound 加上 Type II/III 两个 bilinear-energy 目标即可推出完整 no-RH minor-arc cosine bound。最终层新增的 `Gdbh.strongGoldbach_under_RH_unconditional_realChebyshevPNT_exceptional_quadratic_zero_pageSiegel_swT3_bilinearEnergy` 使用这个更低层 Vinogradov 输入，是 Type I 下沉前的 no-RH handoff：Page-Siegel/PNT、Type I、Type II energy、Type III energy、局部 SW T3 smallness 和有限阈值覆盖一旦给出，就能推出 `StrongGoldbach`。

新增的 `Gdbh.PathAMinorArc.TypeILinearEstimateFor` 把剩余 Type I witness 黑盒下沉为“一重有限和表示 + summation-by-parts 后绝对值估计”。Lean 证明 `typeISumBoundFor_of_linearEstimate` 用有限和三角不等式把该低层目标转回 `TypeISumBoundFor`，`vinogradovTypeIBoundForVaughanWitnessUnconditional_of_linearEstimate` 关闭现有 Type I witness bound，`minorArcCosineSumBound_of_vinogradov_typeI_linear_typeII_typeIII_bilinearEnergies` 表明 Type I linear estimate 加上 Type II/III bilinear-energy estimates 即可推出完整 no-RH minor-arc bound。最终层新增的 `Gdbh.strongGoldbach_under_RH_unconditional_realChebyshevPNT_exceptional_quadratic_zero_pageSiegel_swT3_vinogradovEnergy` 是 T3 threshold 下沉前的全 Vinogradov 低层 handoff：Page-Siegel/PNT、Type I linear、Type II energy、Type III energy、局部 SW T3 smallness 和有限阈值覆盖一旦给出，就能推出 `StrongGoldbach`。剩余 Vinogradov 缺口不再是 Lean 的 witness-bound 胶水，而是证明 Vaughan Type I 的具体线性表示及其几何和/分部求和估计，并证明 Type II/III 的具体双线性能量估计。

新增的 `Gdbh.PathA_T3SWThresholdSmallnessForFareyWitness` 把 T3 局部小量接口再下沉一层：SW 形状 major-arc 估计从 `N₀` 开始即可，但 `siegelWalfiszErrorFn C c n ≤ δ_M * 𝔖(n) * n` 的最终小量比较可以有独立阈值 `N_M`，minor contribution 小量也有独立阈值 `N_m`。Lean 证明 `PathA_T3FareyWitnessAggregation_of_swThresholdSmallness` 会把 Farey witness 的阈值提升到 `max N₀ N_M`，再组装出旧的 Farey-witness aggregation；`pathA_quantitativeContent_of_T3SWThresholdSmallness` 和 `exists_quarterBinaryHardyLittlewoodLowerBound_of_T3SWThresholdSmallness` 则直接进入最终 Hardy-Littlewood content。最终层新增的 `Gdbh.strongGoldbach_under_RH_unconditional_realChebyshevPNT_exceptional_quadratic_zero_pageSiegel_swT3Threshold_vinogradovEnergy` 现在是最紧的 no-RH handoff：Page-Siegel/PNT、Type I linear、Type II energy、Type III energy、thresholded SW T3 smallness 和有限阈值覆盖一旦给出，就能推出 `StrongGoldbach`。这把 Prop 4 的剩余任务从“给定 major 阈值立即小”修正为更实际的“允许 eventual major/minor 小量阈值后再提升 witness 阈值”。

新增的 `Gdbh.ZetaZeroFreeRegionAndChebyshevPsiRealPNTRemainder` 把 PNT 侧改成可直接交接的 theorem package：同时给出 zeta 零点自由区域和实变量 `Chebyshev.psi` 的 `exp(-c * sqrt(log x))` 余项。Lean 证明 `zetaZeroFreeRegionForPNT_of_zetaZeroFreeRegionAndChebyshevPsiRealPNTRemainder`、`zetaZeroFreeRegionToChebyshevPsiRealPNTRemainderBridge_of_zetaZeroFreeRegionAndChebyshevPsiRealPNTRemainder` 与 `pntRemainder_of_zetaZeroFreeRegionAndChebyshevPsiRealPNTRemainder`，因此最终层新增的 `Gdbh.strongGoldbach_under_RH_unconditional_pntRealRemainder_exceptional_quadratic_zero_pageSiegel_swT3Threshold_vinogradovEnergy` 可以直接消费这个 PNT package、quadratic exceptional-zero Page-Siegel、Type I linear、Type II/III energy、thresholded SW T3 smallness 和有限阈值覆盖。这里的 bridge-shaped 结论是由 package 中已经给出的 real-Chebyshev 余项适配而来，不是在本仓库内证明 zeta 零点自由区域推出 PNT；因此这仍然是无公理的 handoff 精化，不是完整数学证明。

新增的 `Gdbh.PrimitivePageSiegelExceptionalQuadraticZeroAndSiegelWalfiszNonPrincipal` 对 Page-Siegel 侧做同样的 theorem package 下沉：同时给出 quadratic exceptional-zero Page-Siegel 零点自由区域和非主特征 `SiegelWalfiszNonPrincipal` 结论。Lean 证明 `primitivePageSiegelExceptionalQuadraticZero_of_pageSiegelNonPrincipalPackage`、`siegelWalfiszNonPrincipal_of_pageSiegelNonPrincipalPackage` 与 `primitivePageSiegelExceptionalQuadraticZeroToSiegelWalfiszNonPrincipalBridge_of_pageSiegelNonPrincipalPackage`，因此最终层新增的 `Gdbh.strongGoldbach_under_RH_unconditional_pntRealRemainder_quadraticZero_pageSiegelNonPrincipal_swT3Threshold_vinogradovEnergy` 现在直接消费 PNT package、Page-Siegel non-principal package、Type I linear、Type II/III energy、thresholded SW T3 smallness 和有限阈值覆盖。这里的 bridge-shaped 结论同样是由 package 中已经给出的非主项 SW 定理适配而来，不是在本仓库内证明 Page-Siegel 零点自由区域推出 Siegel-Walfisz；因此这是最强 handoff 的开放字段压缩与交接精化，而不是关闭 Page-Siegel 数学缺口。

新增的 `Gdbh.PathA_MinorArcT3SWThresholdPackage` 把 Vinogradov/T3 侧的最终交接再压缩成 theorem-level package：包含 `SiegelWalfiszPerArcToFourierAggregation`、已经组装好的 `MinorArcCosineSumBound`，以及 `PathA_T3SWThresholdSmallnessForFareyWitness`。Lean 证明 `PathA_HardyLittlewoodSmallnessForFareyWitness_of_minorArcT3SWThresholdPackage` 可在给定完整 `SiegelWalfiszBound` 后直接产出 Farey-witness Hardy-Littlewood smallness，并由 `pathA_quantitativeContent_of_minorArcT3SWThresholdPackage` 与 `exists_quarterBinaryHardyLittlewoodLowerBound_of_minorArcT3SWThresholdPackage` 进入最终 lower-bound 桥。最终层新增的 `Gdbh.strongGoldbach_under_RH_unconditional_pntRealRemainder_quadraticZero_pageSiegelNonPrincipal_minorArcT3` 现在只暴露三个主要开放 theorem package：PNT real remainder package、Page-Siegel non-principal package、minor-arc/T3 package，外加有限阈值覆盖。这保留了 Type I linear 与 Type II/III bilinear-energy 的低层接口供后续 mathlib formalization 使用，但最终 wrapper 不再把三个 Vinogradov 字段作为单独最终字段。

新增的 `Gdbh.PathAMinorArc.VinogradovVaughanBilinearFormalizationPackage` 给 Vinogradov 侧也命名了单一低层 formalization package：Type I linear estimate、Type II bilinear-energy estimate、Type III bilinear-energy estimate 三者同时给出。Lean 证明 `minorArcCosineSumBound_of_vinogradov_bilinearFormalizationPackage` 可直接组装出 no-RH `MinorArcCosineSumBound`；最终层新增的 `Gdbh.PathA_VinogradovT3SWThresholdPackage` 再把 SW-to-Fourier aggregation、这个 Vinogradov package 和 thresholded SW T3 smallness 组合起来，并由 `minorArcT3SWThresholdPackage_of_vinogradovT3SWThresholdPackage` 转成已组装的 minor-arc/T3 package。新的最终 wrapper `Gdbh.strongGoldbach_under_RH_unconditional_pntRealRemainder_quadraticZero_pageSiegelNonPrincipal_vinogradovT3` 因而直接消费 PNT package、Page-Siegel non-principal package、lowered Vinogradov/T3 package 和有限阈值覆盖。这把 Props 5+6 的剩余正式目标收束为一个可交付的 Vinogradov theorem package，同时保留最终层可以选择消费更高层 minor-arc/T3 package 的路线。

新增的 `Gdbh.PathA_T3SWQuarterSmallnessPackage` 把 Prop 4 的 thresholded T3 smallness 再拆成两个可分别交付的 theorem package：`PathA_T3SWMajorErrorQuarterSmallness` 要求 SW 形状主弧误差最终不超过主项的四分之一，`PathA_T3MinorContributionQuarterSmallnessForFareyWitness` 要求给定 SW Farey witness 和 assembled Vinogradov minor-arc bound 后，minor contribution 最终不超过主项的四分之一。Lean 证明 `PathA_T3SWThresholdSmallnessForFareyWitness_of_quarterSmallnessPackage` 可取 `δ_M = δ_m = 1/4` 回到现有 thresholded T3 contract。最终层新增的 `Gdbh.PathA_VinogradovT3SWQuarterSmallnessPackage` 把 SW-to-Fourier aggregation、full lowered Vinogradov package 和 split-quarter T3 package 组合起来，并由 `vinogradovT3SWThresholdPackage_of_vinogradovT3SWQuarterSmallnessPackage` 回到上一层 package。新的 wrapper `Gdbh.strongGoldbach_under_RH_unconditional_pntRealRemainder_quadraticZero_pageSiegelNonPrincipal_vinogradovT3Quarter` 因而把 Prop 4 的最终剩余接口暴露为两个具体的小量比较目标，而不是一个 bundled threshold-smallness 黑盒。

新增的 `Gdbh.PathAMinorArc.TypeIIBilinearSeparatedEnergyEstimateFor` 把 Vinogradov Type II/III 的 post-Cauchy-Schwarz energy target 再下沉一层：未来证明可以分别给出系数能量乘积上界 `E_coeff`、kernel 能量上界 `E_kernel`，以及最终乘积比较 `E_coeff * E_kernel ≤ target²`。Lean 证明 `typeIIBilinearEnergyEstimateFor_of_separatedEnergyEstimate` 可把这个 separated form 组装回旧的 bilinear-energy target，并给出 Type II/III witness 的专门桥。新的 `Gdbh.PathAMinorArc.VinogradovVaughanSeparatedBilinearFormalizationPackage` 将 Type I linear estimate 与 Type II/III separated energy estimates 打包，`minorArcCosineSumBound_of_vinogradov_separatedBilinearFormalizationPackage` 仍可推出 no-RH minor-arc bound。最终层新增 `Gdbh.PathA_VinogradovSeparatedT3SWQuarterSmallnessPackage` 和 wrapper `Gdbh.strongGoldbach_under_RH_unconditional_pntRealRemainder_quadraticZero_pageSiegelNonPrincipal_vinogradovSeparatedT3Quarter`，让当前最细 handoff 同时暴露 Props 5+6 的 coefficient/kernel energy 目标和 Prop 4 的 split-quarter T3 目标。

新增的 `Gdbh.PathAMinorArc.TypeILinearSeparatedEstimateFor` 把 Vinogradov Type I 的 linear estimate 也拆成 coefficient mass、kernel sup 和最终乘积比较三部分；Lean 证明 `typeILinearEstimateFor_of_separatedEstimate` 可把该 separated linear form 组装回旧 Type I linear target，并给出 Vaughan witness 的专门桥。新的 `Gdbh.PathAMinorArc.VinogradovVaughanFullySeparatedFormalizationPackage` 现在同时暴露 Type I separated linear estimate 与 Type II/III separated bilinear-energy estimates，`minorArcCosineSumBound_of_vinogradov_fullySeparatedFormalizationPackage` 仍可推出 no-RH minor-arc bound。最终层新增 `Gdbh.PathA_VinogradovFullySeparatedT3SWQuarterSmallnessPackage` 和 wrapper `Gdbh.strongGoldbach_under_RH_unconditional_pntRealRemainder_quadraticZero_pageSiegelNonPrincipal_vinogradovFullySeparatedT3Quarter`，让当前最细 handoff 同时分离所有三个 Vinogradov 输入和 Prop 4 的 split-quarter T3 目标。

新增的 `Gdbh.PathA_T3SWMajorErrorQuarterSmallness_holds` 关闭了 split-quarter T3 中的 SW major-error quarter-smallness：Lean 使用 `tendsto_sqrtLogExp_nat_atTop_zero` 证明 `exp(-c * sqrt(log n))` 衰减到 0，再结合既有 singular-series 正下界把 SW 误差最终压到主项四分之一以内。新的 `Gdbh.PathA_VinogradovFullySeparatedT3MinorQuarterSmallnessPackage` 因而只保留 `PathA_T3MinorContributionQuarterSmallnessForFareyWitness` 作为 T3 字段，并通过 `vinogradovFullySeparatedT3SWQuarterSmallnessPackage_of_vinogradovFullySeparatedT3MinorQuarterSmallnessPackage` 回到旧 fully separated quarter package。最终层新增 wrapper `Gdbh.strongGoldbach_under_RH_unconditional_pntRealRemainder_quadraticZero_pageSiegelNonPrincipal_vinogradovFullySeparatedT3MinorQuarter`，把 Prop 4 的剩余公开目标压缩为 minor contribution quarter-smallness；这仍未证明 minor contribution 本身，只是把 SW major-error 小量比较从开放假设中移除。

新增的 `Gdbh.PathA_T3MinorContributionDftUniformSquareQuarterSmallnessForFareyWitness` 把上述剩余 T3 字段再下沉到 Fourier 侧：对给定 Farey major arcs，只需在 minor frequencies 上给出统一的 DFT bound，并证明该 bound 的平方最终不超过 `1/4 * 𝔖(n) * n`。Lean 证明 `PathA_T3MinorContributionQuarterSmallnessForFareyWitness_of_dftUniformSquareQuarterSmallness` 使用既有 `DiscreteCircleMethod.minorArcComplexContributionBound_of_dft_bound_sq_of_uniform_bound` 和实部范数不等式，把这个 DFT-square 目标转回 real minor contribution quarter-smallness。新的 `Gdbh.PathA_VinogradovFullySeparatedT3DftUniformMinorQuarterSmallnessPackage` 与 wrapper `Gdbh.strongGoldbach_under_RH_unconditional_pntRealRemainder_quadraticZero_pageSiegelNonPrincipal_vinogradovFullySeparatedT3DftUniformMinorQuarter` 因而把 Prop 4 的剩余公开目标改成更接近 circle-method minor-arc 估计的 uniform DFT-square 小量目标；这仍未证明该 DFT bound，只是把 real contribution bookkeeping 从开放假设中移除。

新增的 `Gdbh.PathA_T3MinorContributionDftUniformLittleOForFareyWitness` 把 DFT-square 小量比较再改成极限形式：解析侧只需给出同一个 minor-frequency uniform DFT bound，并证明 `minorArcDftBound(n) / sqrt(n) -> 0`。Lean 证明 `exists_dftUniformBound_sq_le_quarter_singularSeries_of_littleO` 会从该 little-o 结论抽取阈值，并结合 `one_fourth_le_goldbachSingularSeriesFromQuarter` 得到 `minorArcDftBound(n)^2 <= 1/4 * 𝔖(n) * n`；`PathA_T3MinorContributionDftUniformSquareQuarterSmallnessForFareyWitness_of_dftUniformLittleO` 再把它接回上一层 DFT-square package。新的 `Gdbh.PathA_VinogradovFullySeparatedT3DftUniformLittleOMinorPackage` 与 wrapper `Gdbh.strongGoldbach_under_RH_unconditional_pntRealRemainder_quadraticZero_pageSiegelNonPrincipal_vinogradovFullySeparatedT3DftUniformLittleOMinor` 因而把 Prop 4 的剩余公开目标压成更标准的 `o(sqrt n)` minor DFT estimate；这仍未证明该 uniform DFT little-o 定理，只是把从 little-o 到 quarter budget 的阈值和奇异级数 bookkeeping 从开放假设中移除。

新增的 `Gdbh.PathA_T3MinorContributionDftUniformComplementLittleOForFareyWitness` 把同一个 little-o DFT 目标改写成更贴近解析证明的补集形式：未来 T3/major-arc 工作只需在 `k ∉ majorArcs n` 上给出 uniform DFT bound 和 `o(sqrt n)` 极限，而不必直接使用项目内部的 `zmodMinorFrequencies` finset。Lean 证明 `PathA_T3MinorContributionDftUniformLittleOForFareyWitness_of_complementLittleO` 使用 `DiscreteCircleMethod.minorArcDftBoundValid_of_not_mem_majorArcs` 将补集估计转回 minor-frequency 估计，再沿用已有 little-o-to-square 与 square-to-real-minor 桥。新的 `Gdbh.PathA_VinogradovFullySeparatedT3DftUniformComplementLittleOMinorPackage` 与 wrapper `Gdbh.strongGoldbach_under_RH_unconditional_pntRealRemainder_quadraticZero_pageSiegelNonPrincipal_vinogradovFullySeparatedT3DftUniformComplementLittleOMinor` 因而把 Prop 4 的公开接口进一步整理为“major arcs 的补集上统一 DFT 为 `o(sqrt n)`”；这仍不是该估计的证明。

新增的 `Gdbh.minorArcDftSupNormForFareyWitness` 和 `Gdbh.PathA_T3MinorContributionDftSupComplementLittleOForFareyWitness` 去掉了上一层接口中任意选择 `minorArcDftBound` 的自由度：现在最细 T3 目标是证明给定 major arcs 补集上的实际有限上确界/最大 DFT 范数本身满足 `minorArcDftSupNormForFareyWitness(majorArcs,n) / sqrt(n) -> 0`。Lean 证明 `vonMangoldtZModDft_norm_le_minorArcDftSupNormForFareyWitness` 和 `PathA_T3MinorContributionDftUniformComplementLittleOForFareyWitness_of_supComplementLittleO` 会把这个 finite-supremum little-o 目标转成 uniform complement little-o，再接回旧链条。新的 `Gdbh.PathA_VinogradovFullySeparatedT3DftSupComplementLittleOMinorPackage` 与 wrapper `Gdbh.strongGoldbach_under_RH_unconditional_pntRealRemainder_quadraticZero_pageSiegelNonPrincipal_vinogradovFullySeparatedT3DftSupComplementLittleOMinor` 因而把 Prop 4 的剩余公开目标压成“实际 minor DFT max norm 是 `o(sqrt n)`”；这仍未证明该 max-norm 估计。

新增的 `Gdbh.PathAMinorArc.typeI_sum_eq_divisorsAntidiagonal_sum` 和 `Gdbh.PathAMinorArc.vaughanS_I_witness_eq_divisorsAntidiagonal_sum` 把 Vinogradov Type I 的 Vaughan witness 真实展开式固定到 Dirichlet convolution 的 `Nat.divisorsAntidiagonal` 有限和。新的 `Gdbh.PathAMinorArc.VinogradovTypeIDivisorAntidiagonalEstimateForVaughanWitness` 不再要求未来证明同时提供 Type I 表示；它只要求对这个实际展开式证明 `C * sqrt(N) * log(N)^k` 级别的定量界。Lean 证明 `vinogradovTypeIBoundForVaughanWitnessUnconditional_of_divisorAntidiagonalEstimate` 和 `minorArcCosineSumBound_of_vinogradov_typeIDivisorAntidiagonalFormalizationPackage` 会把该 Type I 展开估计与 Type II/III separated bilinear energy 目标重新组装成 no-RH minor-arc cosine bound。最终层新增的 `Gdbh.PathA_VinogradovTypeIDivisorAntidiagonalT3DftSupComplementLittleOMinorPackage` 与 wrapper `Gdbh.strongGoldbach_under_RH_unconditional_pntRealRemainder_quadraticZero_pageSiegelNonPrincipal_vinogradovTypeIDivisorAntidiagonalT3DftSupComplementLittleOMinor` 因而把当前 Vinogradov/T3 交接改成“Type I 具体展开式估计 + Type II/III 分离能量 + T3 实际 minor DFT max norm little-o”；这仍未证明 Type I 的平方根级估计，只是把表示 bookkeeping 从开放假设中移除。

新增的 `Gdbh.PathAMinorArc.typeII_sum_eq_divisorsAntidiagonal_sum`、`Gdbh.PathAMinorArc.vaughanS_II_witness_eq_divisorsAntidiagonal_sum`、`Gdbh.PathAMinorArc.typeIII_sum_eq_divisorsAntidiagonal_sum` 与 `Gdbh.PathAMinorArc.vaughanS_III_witness_eq_divisorsAntidiagonal_sum` 继续把 Type II/III 也固定到 Vaughan 展开后的嵌套 `Nat.divisorsAntidiagonal` 有限和。新的 `Gdbh.PathAMinorArc.VinogradovVaughanDivisorAntidiagonalFormalizationPackage` 因而同时暴露 Type I/II/III 三个实际展开式的定量估计，Lean 证明 `minorArcCosineSumBound_of_vinogradov_divisorAntidiagonalFormalizationPackage` 会把它们组装回 no-RH minor-arc cosine bound。最终层新增 `Gdbh.PathA_VinogradovDivisorAntidiagonalT3DftSupComplementLittleOMinorPackage` 与 wrapper `Gdbh.strongGoldbach_under_RH_unconditional_pntRealRemainder_quadraticZero_pageSiegelNonPrincipal_vinogradovDivisorAntidiagonalT3DftSupComplementLittleOMinor`，让当前最细交接变成“三个 Vaughan 展开式的定量估计 + T3 实际 minor DFT max norm little-o”；这些估计本身仍是开放解析输入。

新增的 `Gdbh.PathAMinorArc.nested_abs_sum_mul_sq_le_sum_sq_mul_sum_sq`、`Gdbh.PathAMinorArc.typeII_divisorAntidiagonal_expanded_sum_eq_coeff_kernel`、`Gdbh.PathAMinorArc.typeIII_divisorAntidiagonal_expanded_sum_eq_coeff_kernel` 和 `Gdbh.PathAMinorArc.divisorAntidiagonalSumBoundFor_of_separatedEnergyEstimate` 把上述 Type II/III 实际展开式再接入 Cauchy-Schwarz 后的 coefficient/kernel energy 形态。新的 `Gdbh.PathAMinorArc.VinogradovVaughanTypeIDivisorAntidiagonalSeparatedEnergyFormalizationPackage` 保留 Type I 的实际展开式估计，同时要求对 Type II/III 的实际展开式分别给出系数能量、kernel 能量和乘积比较；Lean 证明 `minorArcCosineSumBound_of_vinogradov_typeIDivisorAntidiagonalSeparatedEnergyPackage` 会先恢复 fully-expanded package 再组装 minor-arc bound。最终层新增 `Gdbh.PathA_VinogradovTypeIDivisorAntidiagonalSeparatedEnergyT3DftSupComplementLittleOMinorPackage` 与 wrapper `Gdbh.strongGoldbach_under_RH_unconditional_pntRealRemainder_quadraticZero_pageSiegelNonPrincipal_vinogradovTypeIDivisorAntidiagonalSeparatedEnergyT3DftSupComplementLittleOMinor`，让当前最细 Vinogradov/T3 交接变成“Type I 实际展开式估计 + Type II/III 实际展开式的 separated energy + T3 实际 minor DFT max norm little-o”；这些 energy bounds 仍是开放解析输入。

新增的 `Gdbh.PathAMinorArc.typeI_divisorAntidiagonal_expanded_sum_eq_coeff_kernel`、`Gdbh.PathAMinorArc.VinogradovTypeIDivisorAntidiagonalSeparatedEnergyForVaughanWitness` 和 `Gdbh.PathAMinorArc.vinogradovTypeIDivisorAntidiagonalEstimateForVaughanWitness_of_separatedEnergy` 把 Type I 的实际 divisor-antidiagonal 展开式也接入同一个 coefficient/kernel separated-energy 接口。新的 `Gdbh.PathAMinorArc.VinogradovVaughanDivisorAntidiagonalSeparatedEnergyFormalizationPackage` 因而同时暴露 Type I/II/III 三个实际 Vaughan 展开式的 separated-energy 目标，Lean 证明 `minorArcCosineSumBound_of_vinogradov_divisorAntidiagonalSeparatedEnergyPackage` 会先把 Type I separated energy 恢复为实际展开式估计，再沿用 Type II/III 的 Cauchy-Schwarz 组装。最终层新增 `Gdbh.PathA_VinogradovDivisorAntidiagonalSeparatedEnergyT3DftSupComplementLittleOMinorPackage` 与 wrapper `Gdbh.strongGoldbach_under_RH_unconditional_pntRealRemainder_quadraticZero_pageSiegelNonPrincipal_vinogradovDivisorAntidiagonalSeparatedEnergyT3DftSupComplementLittleOMinor`，让当前最细 Vinogradov/T3 交接变成“三个实际 Vaughan divisor-antidiagonal 展开式的 coefficient/kernel separated energy + T3 实际 minor DFT max norm little-o”；这些 energy bounds 仍是开放解析输入。

新增的 `Gdbh.PathAMinorArc.DivisorAntidiagonalSeparatedEnergyComponentsFor` 把 separated-energy 目标再拆成全局 coefficient-energy 函数、kernel-energy 函数和最终乘积比较。Lean 证明 `divisorAntidiagonalSeparatedEnergyEstimateFor_of_components` 可把这些 component estimates 重新打包为旧的 per-`N, α` separated-energy 形式；新的 `Gdbh.PathAMinorArc.VinogradovVaughanDivisorAntidiagonalSeparatedEnergyComponentsFormalizationPackage` 同时要求 Type I/II/III 三个实际 Vaughan 展开式的 componentized energy。最终层新增 `Gdbh.PathA_VinogradovDivisorAntidiagonalSeparatedEnergyComponentsT3DftSupComplementLittleOMinorPackage` 与 wrapper `Gdbh.strongGoldbach_under_RH_unconditional_pntRealRemainder_quadraticZero_pageSiegelNonPrincipal_vinogradovDivisorAntidiagonalSeparatedEnergyComponentsT3DftSupComplementLittleOMinor`，让当前最细 Vinogradov/T3 交接变成“三个实际 Vaughan 展开式各自的全局 coefficient-energy 函数、kernel-energy 函数和乘积比较 + T3 实际 minor DFT max norm little-o”；这些 component estimates 仍是开放解析输入。

新增的 `Gdbh.PathAMinorArc.divisorAntidiagonalCoeffEnergy`、`Gdbh.PathAMinorArc.divisorAntidiagonalKernelEnergy` 和 `Gdbh.PathAMinorArc.DivisorAntidiagonalExactEnergyProductEstimateFor` 把 componentized separated-energy 目标进一步固定到实际平方能量函数。Lean 证明 `divisorAntidiagonalSeparatedEnergyComponentsFor_of_exactEnergyProduct` 可取这些实际能量函数恢复 componentized 接口；新的 `Gdbh.PathAMinorArc.VinogradovVaughanDivisorAntidiagonalExactEnergyFormalizationPackage` 同时要求 Type I/II/III 三个实际 Vaughan 展开式的 exact-energy product estimate。最终层新增 `Gdbh.PathA_VinogradovDivisorAntidiagonalExactEnergyT3DftSupComplementLittleOMinorPackage` 与 wrapper `Gdbh.strongGoldbach_under_RH_unconditional_pntRealRemainder_quadraticZero_pageSiegelNonPrincipal_vinogradovDivisorAntidiagonalExactEnergyT3DftSupComplementLittleOMinor`，让当前最细 Vinogradov/T3 交接变成“三个实际 Vaughan 展开式的实际 coefficient square-energy × actual kernel square-energy 乘积估计 + T3 实际 minor DFT max norm little-o”；这些 product estimates 仍是开放解析输入。

新增的 `Gdbh.PathAMinorArc.TypeIIBilinearInnerEnergyEstimateFor` 把 Vinogradov Type II/III 的 bilinear handoff 改写为第一步 Cauchy-Schwarz 后的 inner oscillatory square-energy 形态：Lean 证明 `typeIISumBoundFor_of_innerBilinearEnergyEstimate` 直接从 `∑ a_i² · ∑_i |∑_j b_{ij}K_{ij}|²` 型估计恢复 Type II bound，而不是要求 flattened `∑ b_j² · ∑ K_{ij}²` 能量。新的 `Gdbh.PathAMinorArc.VinogradovVaughanInnerBilinearFormalizationPackage` 和最终层 `Gdbh.PathA_VinogradovInnerBilinearT3DftSupComplementLittleOMinorPackage`/wrapper `Gdbh.strongGoldbach_under_RH_unconditional_pntRealRemainder_quadraticZero_pageSiegelNonPrincipal_vinogradovInnerBilinearT3DftSupComplementLittleOMinor` 让 Props 5+6 的可交接目标更接近实际 Vinogradov proof shape：Type I linear estimate + Type II/III first-CS inner-sum energy estimates + T3 actual minor DFT max norm little-o。这里仍未证明 rational approximation/geometric-sum estimates；只是把 Lean 胶水移到更合适的 inner-energy 接口。

缺少对无限多个偶数的无条件数学证明。Lean 文件中的 `Gdbh.Round1Status.strongGoldbach_iff_currentFormalTarget50000` 证明了当前最强有限归约目标精确等价于 `Gdbh.ExplicitGoldbachLowerBound 50000`，也就是“50000 以上每个偶数的 Goldbach 表示计数为正”；旧的 `strongGoldbach_iff_explicit_lower_bound100` 仍保留为 legacy 100 阈值等价。当前最适合继续接入解析数论估计的交接对象是 `VonMangoldtSplitThresholdPositiveLinearWeightSumMajorMinorArcEstimate`、`VonMangoldtEventuallyPositiveLinearWeightSumMajorMinorArcEstimate`、`VonMangoldtEventuallyRelativeErrorSqrtLogCountMajorMinorArcEstimate`、`VonMangoldtSplitThresholdRelativeErrorSqrtLogCountMajorMinorArcEstimate`、`VonMangoldtEventuallyRelativeErrorWeightSumMajorMinorArcEstimate`、`VonMangoldtSplitThresholdRelativeErrorWeightSumMajorMinorArcEstimate`、`VonMangoldtRelativeErrorWeightSumMajorMinorArcEstimate`、`VonMangoldtEventuallyRelativeErrorPositiveLinearRawLowerBound`、`VonMangoldtSplitThresholdPositiveLinearRawWeightSumLowerBound`、`VonMangoldtEventuallyPositiveLinearRawWeightSumLowerBound`、`VonMangoldtRelativeErrorPositiveLinearRawWeightSumLowerBound`、`VonMangoldtPositiveLinearRawWeightSumLowerBound`、`VonMangoldtEventuallyDirectRawWeightSumLowerBound`、`VonMangoldtSplitThresholdDirectRawWeightSumLowerBound`、`VonMangoldtSplitThresholdDirectWeightSumMajorMinorArcEstimate`、`VonMangoldtEventuallyDirectWeightSumMajorMinorArcEstimate`、`VonMangoldtDirectWeightSumMajorMinorArcEstimate`、`VonMangoldtEventuallyPositiveLinearMajorMinorArcEstimate`、`VonMangoldtSqrtLogCountMajorMinorArcEstimate`、`VonMangoldtEventuallyHardyLittlewoodNormalizedEstimate`、`VonMangoldtHardyLittlewoodNormalizedEstimate`、`VonMangoldtHardyLittlewoodNormalizedCanonicalWeightSumEstimate`、`VonMangoldtHardyLittlewoodNormalizedWeightSumEstimate` 或更紧的 `VonMangoldtDirectRawWeightSumLowerBound`，其中污染项乘积 bookkeeping、`Λ(m) <= log n` 因子上界、非素数素数幂计数上界 `NonPrimePrimePowerCount n <= (sqrt n + 1)*(log₂ n + 1)`、非素数素数幂 `Λ` 权重和交接、相对误差 raw Λ 下界到有效正线性系数的转移、major/minor arc 总解析误差占比到 raw 相对误差下界的转移、relative-error major/minor 到正线性 net 下界的转移、实变量模型 `sqrt(x)*(log x)^3 = o(x)`，以及该模型到精确自然数污染预算的 eventually sublinear 转移都已由 Lean 处理；但它仍需要具体、无条件、带有效常数的 raw Λ major/minor arc 估计或直接 raw Λ 卷积下界。direct raw weight-sum 接口要求证明 `NonPrimePrimePowerVonMangoldtWeightSum n <= weightSumBound n` 和 `2 * weightSumBound n * log n < RawVonMangoldtGoldbachSum n`；split-threshold raw 版本允许这两条有两个显式阈值，Lean 自动取最大值；eventually raw 版本允许这两条都先以 `∀ᶠ n in atTop` 形式给出，Lean 自动抽取共同阈值；positive-linear raw weight-sum 版本允许把目标拆成 `RawVonMangoldtGoldbachSum n >= coefficient * n` 与 `2 * weightSumBound n * log n < coefficient * n`；relative-error raw 版本允许先证明 `RawVonMangoldtGoldbachSum n >= (1-delta)*mainTerm n` 和 `mainTerm n >= c*n`，Lean 自动使用有效系数 `(1-delta)*c`；eventually relative-error raw 版本允许这两条只以最终成立形式给出，Lean 会抽取共同阈值并自动进入 sqrt-log 污染预算；relative-error major/minor 版本允许先证明 `mainTerm - majorArcError <= RawVonMangoldtGoldbachSum + minorArcError` 和 `majorArcError + minorArcError <= delta*mainTerm`，Lean 自动推出 raw 相对误差下界；split-threshold relative-error major/minor 版本允许 main、combined、total error、weight-sum 和 contamination 五条估计各有显式阈值，Lean 自动取最大值；eventually relative-error major/minor 版本允许这五条估计只以 `∀ᶠ n in atTop` 形式给出，Lean 会合并并抽取共同阈值；eventually relative-error sqrt-log count 版本只要求 main、combined、total error 三条 eventually 条件，并由 Lean 自动使用 sqrt-log count 污染预算的次线性定理；split-threshold relative-error sqrt-log count 版本允许 main、combined、total error 三条条件有显式阈值，Lean 先取 `max mainTermThreshold (max combinedThreshold totalAnalyticErrorThreshold)`，再自动加入 sqrt-log count 污染预算支配阈值；canonical HL weight-sum 版本只要求显式奇异级数下界和归一化误差界，Lean 自动加入 canonical weight-sum 上界与 eventual 污染支配，但仍需要覆盖派生的 direct raw weight-sum 阈值；这两个 sqrt-log relative-error 版本现在也都有阈值不超过 100 时直接使用 legacy 100 证书的强猜想桥，包括普通 sqrt-log 阈值和 weight-sum 终局阈值两种形式；split-threshold positive-linear raw 版本允许三条 raw weight-sum 条件有三个显式阈值，eventually 版本会合并 weight-sum 上界、正线性 raw 下界和污染预算支配三条最终条件并抽取共同阈值；eventually positive-linear weight-sum major/minor 版本则从 `mainTerm - majorArcError <= RawVonMangoldtGoldbachSum + minorArcError` 和 `coefficient*n + minorArcError <= mainTerm - majorArcError` 先消去 minor error，再携带任意 `weightSumBound` 污染预算进入终局桥；split-threshold positive-linear weight-sum major/minor 版本允许 combined、linear-net、weight-sum、contamination 四条估计各有显式阈值，Lean 先把前两条合成 raw 线性阈值，再转入三阈值 raw weight-sum 桥。direct weight-sum major/minor 接口要求证明 `mainTerm n - majorArcError n <= RawVonMangoldtGoldbachSum n + minorArcError n`、证明 `NonPrimePrimePowerVonMangoldtWeightSum n <= weightSumBound n`，并证明 `majorArcError n + minorArcError n + 2 * weightSumBound n * log n < mainTerm n`；split-threshold major/minor 版本允许这三条有三个显式阈值，Lean 自动取最大值；eventually direct 版本允许这三条都先以 `∀ᶠ n in atTop` 形式给出，Lean 会用 `vonMangoldtEventuallyDirectWeightSumMajorMinorArcEstimate_conditions_eventually` 合并并抽取共同阈值，且现在也有 `strongGoldbach_of_vonMangoldt_eventually_direct_weight_sum_major_minor_arc_estimate_le100` 直接使用 legacy 100 证书。eventually major/minor 接口要求证明 `mainTerm n - majorArcError n <= RawVonMangoldtGoldbachSum n + minorArcError n` 和 `coefficient * n + minorArcError n <= mainTerm n - majorArcError n` 最终成立，其中 `coefficient > 0`。等价地，还可以直接证明更压缩的 raw Λ 卷积下界 `2 * vonMangoldtLogCountContaminationBudget nonPrimePrimePowerSqrtLogCountBound n < RawVonMangoldtGoldbachSum n`，或证明 weight-sum 版本的 `2 * weightSumBound n * log n < RawVonMangoldtGoldbachSum n`。`strongGoldbach_iff_finite_and_above` 还形式化说明：对任意 `B ≥ 2`，完整证明等价于 `GoldbachUpTo B` 与 `GoldbachAbove B` 两部分同时成立。有限验证、弱哥德巴赫、Chen 定理、Hardy-Littlewood 预测都不能单独推出强哥德巴赫猜想。

`VonMangoldtEventuallyAbsErrorPositiveLinearRawLowerBound` 也是同一条最有希望路线的入口：它允许解析侧以绝对值误差形式给出最终估计，Lean 负责把它转换成 eventually relative-error raw 下界并继续使用 sqrt-log 污染预算。
`VonMangoldtEventuallyLittleOAbsErrorPositiveLinearRawLowerBound` 进一步覆盖文献中的 little-o 渐近写法：解析侧证明绝对误差是 `o(mainTerm)` 且主项最终正线性后，Lean 自动取 `relativeError = 1/2` 并转入 abs-error raw 桥。
`VonMangoldtEventuallyAsymptoticEquivalentPositiveLinearRawLowerBound` 覆盖等价渐近写法：解析侧证明 `Raw ~ mainTerm` 且主项最终正线性后，Lean 自动展开为 little-o 误差并继续使用同一条污染预算链。
`VonMangoldtEventuallyNormalizedErrorPositiveLinearRawLowerBound` 覆盖归一化误差写法：解析侧证明 `(Raw-main)/main -> 0` 后，Lean 从邻域球取最终误差 `1/2`，再转入 abs-error raw 桥。
`VonMangoldtEventuallyHardyLittlewoodNormalizedEstimate` 覆盖 Hardy-Littlewood/Vinogradov 常见主项写法：解析侧证明 `singularSeries(n)` 在偶数上最终有正下界，且 `RawVonMangoldtGoldbachSum` 相对 `singularSeries(n) * n` 的归一化误差趋于 0 后，Lean 自动转入 normalized-error raw 桥。
`VonMangoldtHardyLittlewoodNormalizedEstimate` 是同一路线的显式阈值版：解析侧给出具体 `threshold` 和 `relativeError < 1`，证明所有偶数 `n > threshold` 上 `singularSeries(n) >= coefficient` 且归一化误差绝对值不超过 `relativeError`，Lean 直接得到有效系数 `(1-relativeError)*coefficient` 的正线性 raw Λ 下界。
`raw_normalized_error_bound_of_hardy_littlewood_abs_error_bound` 和 `VonMangoldtHardyLittlewoodAbsErrorCanonicalWeightSumEstimate` 让显式总绝对误差界成为推荐 canonical 路线的一等入口：后续只需证明同一 `singularSeries` 的正下界和点态绝对误差界，Lean 会完成 normalized division 形式和 canonical 污染扣除接口。
`raw_abs_error_bound_of_hardy_littlewood_major_minor_abs_error_bound` 和 `VonMangoldtSplitThresholdHardyLittlewoodMajorMinorAbsErrorCanonicalWeightSumEstimate` 进一步把 major/minor arc ledger 接进来：后续可分别证明 major/minor 绝对误差界和 total-error domination，Lean 自动合并并给出 component-wise final threshold theorem。
`VonMangoldtHardyLittlewoodNormalizedWeightSumEstimate` 是当前最显式的同一路线接口：解析侧还要给出 `NonPrimePrimePowerVonMangoldtWeightSum n <= weightSumBound n` 和 `2*weightSumBound(n)*log n < (1-relativeError)*coefficient*n`，Lean 直接进入 direct raw weight-sum 桥，最终只需证明结构里的 `threshold` 不超过有限证书上界。

## 审计结论

目标尚未完成。当前工作区已经提供了研究、审查和有限验证基础，但没有证明强哥德巴赫猜想。
