# AGENTS.md — Binary Goldbach Lean Formalization

<!-- AUTO:HEADER -->
_Last auto-regenerated: **2026-05-28 05:52Z** by `scripts/regenerate_agents_md.py`. Manual edits to AUTO sections will be overwritten — edit prose outside the markers._
<!-- /AUTO:HEADER -->

Lean 4 + mathlib v4.29.1 attempt at unconditional Binary Goldbach. Two parallel attack paths, both axiom-clean at the headline. **Read this before touching any Lean file. Honesty > progress.**

> AUTO sections in this file are regenerated by `scripts/regenerate_agents_md.py` from authoritative Lean source. Manual edits to AUTO sections WILL be overwritten. Edit only the prose outside the markers, or extend the regen script. A Claude Code PostToolUse hook auto-runs the regen after any change to `Gdbh/**/*.lean`. When invoked, codex must also run the regen at the end of any session that edits Lean files (see `lean-attack-prop` Gate 7).

## Project stats

<!-- AUTO:STATS -->
- `.lean` files under `Gdbh/`: **272**
- Total lines (incl. comments / blank): **234,906**
<!-- /AUTO:STATS -->

## Headlines (axiom-clean as of Phase 30)

<!-- AUTO:HEADLINES -->
| Theorem | File:Line | `#print axioms` |
|---|---|---|
| `Gdbh.strongGoldbach_under_RH_phase5_reduced` | `Gdbh/PathA_Final.lean:7445` | ✅ `[propext, Classical.choice, Quot.sound]` |
| `Gdbh.PathCKGoldbachUnconditional.pathC_kGoldbach_unconditional` | `Gdbh/PathC_KGoldbachUnconditional.lean:270` | ✅ `[propext, Classical.choice, Quot.sound]` |
<!-- /AUTO:HEADLINES -->

Both verified by `scripts/audit_full.sh`. Any change must preserve `[propext, Classical.choice, Quot.sound]` exactly.

## Path A — RH-conditional Binary Goldbach (von Mangoldt + circle method)

Bundle: `structure PathA_Phase5ReducedContent` (22 fields) at `Gdbh/PathA_Final.lean` (regen does not pin the line — search for the `structure` keyword).

**9 genuinely open analytic Props** (canonical list lives in `Gdbh/PathA_StatusPhase24.lean` as `PathA_GenuineOpenProps`):

<!-- AUTO:PATHA_OPEN_PROPS -->
| # | Name | Task | Description |
|---|---|---|---|
| 1 | `psiBound` | P5-T1 | RH-conditional ψ square-root error |
| 2 | `pageLogDistance` | P5-T2 | classical Page zero-free region |
| 3 | `siegelAlone` | P5-T2 | classical Siegel zero |
| 4 | `pntRemainder` | P5-T2 | classical PNT with remainder |
| 5 | `uniformPlancherel` | P5-T3 | uniform Plancherel + char orthogonality |
| 6 | `typeIUnconditional` | P5-T5 | classical Vinogradov Type I |
| 7 | `typeIIUnconditional` | P5-T5 | classical Vinogradov Type II |
| 8 | `typeIIIUnconditional` | P5-T5 | classical Vinogradov Type III |
| 9 | `minorArcVinogradov` | P5-T6 | classical minor-arc bilinear bound |
<!-- /AUTO:PATHA_OPEN_PROPS -->

Other 13 fields are existential shape witnesses, user data, or already closed — see `PathA_Phase5_FieldCatalogue` in `PathA_StatusPhase24.lean`.

## Path C — K-Goldbach via Schnirelmann + Brun (unconditional)

Final form: `∃ K, ∀ n ≥ 2, n = sum of ≤ K elements from {0,1,primes}`. Reduces to **4 named open Props**:

<!-- AUTO:PATHC_OPEN_PROPS -->
| # | Name | File:Line | Meaning |
|---|---|---|---|
| 1 | `BrunGoldbachLocalMainTermRefinedAtSqrtKernel` | `Gdbh/PathC_LocalMainTermRefinedAtSqrtClosure.lean:141` | Halberstam-Richert §3.11 master sieve inequality at z = √n |
| 2 | `SingularSeriesMertens3Bound` | `Gdbh/PathC_BrunGoldbachSingularSeries.lean:287` | Averaged Mertens-3 on the HL singular series (pointwise is FALSE — catch #14) |
| 3 | `AtSqrtFixAStrongToUniversal` | `Gdbh/PathC_FullChainAudit.lean:215` | FixA' universal-in-z upgrade (uniform Brun-Bonferroni over z) |
| 4 | `WeightedSchnirelmannResidualBridge` | `Gdbh/PathC_UnconditionalFixAStrong.lean:187` | FixA' → original Refined absorption (weighted Schnirelmann + (log log n)²) |
<!-- /AUTO:PATHC_OPEN_PROPS -->

All four are classical 19th/20th-century analytic NT. Close all four → `pathC_kGoldbach_FINAL` (single-hypothesis kernel form). Estimated 2000–3000 axiom-clean Lean lines total.

## Hard rules (non-negotiable)

- **Axiom hygiene**: every named theorem on the headline chain must `#print axioms` to **exactly** `[propext, Classical.choice, Quot.sound]`. No other axioms permitted.
- **Banned tokens** in `Gdbh/`: `sorry`, `admit`, `axiom`, `opaque`, `constant`, `unsafe`. Currently **0 occurrences** project-wide (per `audit_lean_source.py`). Keep it that way.
- **No trivial-witness exploitation**: do not close existentials with `k = 0`, `C = ∞`, `S = ∅`, etc. to vacuously satisfy a Prop. Existing structural false-Prop guards will catch this; better to catch it yourself first.
- **File-write isolation**: each agent writes to its own file. Do not edit `PathA_Final.lean` or `PathC_KGoldbachUnconditional.lean` casually — these are headline files; touch only via additive lemma files imported into them.
- **Decompose, don't hand-wave**: every attack on a named Prop must produce ≥1 strictly smaller named sub-Prop (with stable name) before any proof attempt.
- **Persistent sub-agent trees**: subagents may spawn child or grandchild agents for score-positive work with non-overlapping ownership and a report path back to the parent. There is no mandatory close step after dispatch; keep useful descendants open for follow-up context. Close only on ownership drift, non-positive score drift, lost report path, high-score timeout without usable progress, completed delivery with no useful remaining context, or explicit user/controller decision.
- **Refresh AGENTS.md after Lean edits**: run `python3 scripts/regenerate_agents_md.py` (the Claude hook does this automatically; codex must do it manually as Gate 7).

## False-Prop blacklist (caught in Phases 9–22)

Full canonical catalogue in `Gdbh/PathC_FinalSummaryPhase22.lean` as `FalsePropCatchList`.

<!-- AUTO:FALSE_PROPS -->
| # | Task | False Prop | Resolution |
|---|---|---|---|
| 1 | P9-T1 | Twin-prime asymptotic incoherence (p = 2 local density) | Replace by paired sift goldbachSiftedPair |
| 2 | P9-T2 | Naive Brun main term n/(log n)² vs Mertens-3 | Replace by paired pairedBrunFactor z |
| 3 | P10-T2 | Derived Brun main-term incoherence (downstream of P9-T2) | Re-derive via corrected paired form |
| 4 | P11-T1 | Derived incoherence (one chain step further) | Same correction propagation |
| 5 | P13-T1 | BrunGoldbachMainTerm with zero reservoir conditionally false | Switch to positive reservoir n/(log n)² |
| 6 | P17-T5 | Stirling truncation tail not negligible for arbitrary K | Restrict to z ≤ √n regime (PairedBrunStirlingSqrt) |
| 7 | P19-T1 | BrunGoldbachPiKBound zChoice kChoice (honest pi/K choice) | Corrected BrunPiKBound with absorbed pi/K constant |
| 8 | P19-T7 | Intermediate Mertens-gap incoherence | Pair-decoupling refactor |
| 9 | P19-T9 | Intermediate Mertens-gap incoherence (cont.) | Same refactor propagated |
| 10 | P18-T2 | PairedBrunFactorMertensLower natural-valued impossible | Lift to ℝ; use P19-T6 paired-Brun-Mertens third lower |
| 11 | P19-T41 | AssemblyPieceA FALSE at n = 30 (primorial) | AssemblyPieceA_Singular with C ≥ 2 main-term coefficient |
| 12 | P19-T51 | BrunGoldbachPairedMainTermRefinedAtSqrt FALSE along primorials | P20-T2 FixA reservoir n · log log n / (log n)² |
| 13 | P20-T3 | FixA AtSqrt with C₁ = 1 FALSE at n = 2310 | P20-T4 FixA' reservoir n · (log log n)² / (log n)² |
| 14 | P22-T1 honesty | S(n) ≤ K · log log n pointwise FALSE (primorials saturate) | Polynomial (log n)³ bound (this file) + Halberstam-Richert §3.11 |
| 15 | P22-T2 honesty | Direct pointwise composition pBF · S to LogLog fails | Halberstam-Richert §3.11 averaged combinatorial absorption |
<!-- /AUTO:FALSE_PROPS -->

Structural anti-patterns to watch for (paraphrased):

- **Pointwise `S(n) ≤ K · log log n` is FALSE** (saturates at primorials n = 30, 210, 2310, 30030). Use averaged Halberstam-Richert §3.11 form.
- **Naive `n/(log n)²` Brun main term FALSE** vs Mertens-3.
- **Stirling truncation tail FALSE for arbitrary K** — restrict to `z ≤ √n`.
- **`AssemblyPieceA` FALSE at n = 30**, **`BrunGoldbachPairedMainTermRefinedAtSqrt` FALSE along primorials**, **FixA with C₁=1 FALSE at n = 2310**.
- **Natural-valued lower bound impossible**, lift to ℝ.
- **Twin-prime asymptotic incoherent at p=2 local density**.

Before attempting any inequality of the form `f(n) ≤ g(n) · (log log n)^k`, **numerically test it at n ∈ {6, 30, 210, 2310, 30030, 510510, 9699690}** (use `/lean-primorial-check` skill). If it fails at any primorial → it's a false Prop, do not attempt.

## Mathematical landmines (mathlib-specific)

- **`schnirelmannDensity_setOf_prime = 0`** in mathlib — Schnirelmann arg must route through `σ(primesSumset)`, never σ(primes).
- **`ExplicitFormulaBridge` legacy form** was structurally FALSE at `ZS=TE=0`. Phase 5 bundle uses `ExplicitFormulaBridgeFor ZS TE` (function form). Don't resurrect the legacy predicate form.
- **mathlib v4.29.1 gaps you may need to fill locally**: Mertens-2 / Mertens-3 (only Mertens-1 closed), Chebyshev lower bound (only upper), Page-Siegel zero-free region, PNT with remainder. Put any new such results under `Gdbh/MathlibExtras/`.

## Verification chain (run before declaring any progress)

```bash
source "$HOME/.elan/env"            # Lean 4.29.1 via elan
lake build                           # full build, ~40s once warm
python3 audit_lean_source.py         # banned-token scan; must print "OK"
bash scripts/audit_full.sh           # P30-T2 full audit (file/line/theorem counts + 2 headline #print axioms)
python3 scripts/regenerate_agents_md.py   # refresh this file's AUTO sections
```

For a single theorem's axiom dependency (preferred over `audit_lean_axioms.py`):

```bash
echo 'import Gdbh.PathA_Final
#print axioms Gdbh.strongGoldbach_under_RH_phase5_reduced' \
  | lake env lean --stdin
```

## Do NOT run

- `audit_lean_axioms.py` — 30+ minute stall on hundreds of theorems. Use single-theorem `#print axioms` instead.
- `codex` with no subcommand — opens TUI, hangs the shell call. Always `codex exec`.
- Anything that modifies `PathA_Final.lean` or `PathC_KGoldbachUnconditional.lean` without first confirming the diff is **purely additive** (new theorem at end, no edits to existing lines on the headline chain).

## Quick locator (when starting an attack)

- Project status & 4 open Props summary: `Gdbh/PathC_FinalSummaryPhase29.lean`
- Path A 9 open Props summary: `Gdbh/PathA_StatusPhase24.lean`
- Full-chain audit (7 links + 3 named bridges): `Gdbh/PathC_FullChainAudit.lean`
- False-Prop catalogue: `Gdbh/PathC_FinalSummaryPhase22.lean`
- Explicit K bound: `Gdbh/PathC_KEstimate.lean`
- Mission statement & vectors: `goal.md`

## How to invoke codex on this project

**Preferred — use the wrapper** (bakes in the invariants + auto-regens AGENTS.md on exit):

```bash
./scripts/codex_lean.sh "Attack BrunGoldbachLocalMainTermRefinedAtSqrtKernel — \
                         decompose into 2 sub-Props and attempt closure on one."
```

The wrapper sources `~/.elan/env`, asserts AGENTS.md exists, prepends the project's hard constraints (axiom hygiene, banned tokens, primorial check, Gate 1–6 workflow), invokes codex with `--profile lean`, and runs `python3 scripts/regenerate_agents_md.py` after codex returns so AGENTS.md is always fresh.

**Raw form (when you really need full control over flags)**:

```bash
codex exec --profile lean -C /Users/tommy/Downloads/gdbh "<prompt>"
```

In raw form, **your prompt MUST end with**: `After any Gdbh/*.lean edit, run python3 scripts/regenerate_agents_md.py`. Otherwise AGENTS.md drifts out of sync.

The `lean` profile (in `~/.codex/config.toml`) is gpt-5.5 xhigh, workspace-write, never-approval, env-inheriting (so HTTPS_PROXY + ~/.elan/env propagate).
